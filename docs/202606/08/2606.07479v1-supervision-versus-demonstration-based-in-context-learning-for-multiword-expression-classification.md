---
title: Supervision versus Demonstration-Based In-Context Learning for Multiword Expression Classification
title_zh: 监督与基于演示的上下文学习在多词表达分类中的对比
authors: "Sercan Karakaş, Yusuf Şimşek"
date: 2026-06-05
pdf: "https://arxiv.org/pdf/2606.07479v1"
tags: ["query:cross-lang"]
score: 9.0
evidence: 土耳其语轻动词结构字面义与惯用义分类
tldr: 土耳其语惯用轻动词结构（LVC）因与字面动词-宾语组合同形而难以分类。将LVC检测视为二分类任务，比较监督编码器BERTurk与三种指令微调LLM在零样本、一样本和少样本提示下的表现。零样本LLM负例识别好但LVC召回低；一样本提示提升召回但引入模型特定偏差；少样本提示改善校准，GPT-OSS-20B和Qwen 2.5-14B表现稳健。结果显示提示敏感性，精心构造的示例可使LLM匹配或超越监督基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.07479v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 780, \"height\": 654, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07479v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 640, \"height\": 399, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07479v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 667, \"height\": 344, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.07479v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 792, \"height\": 268, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.07479v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 728, \"height\": 173, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.07479v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 677, \"height\": 189, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.07479v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1319, \"height\": 524, \"label\": \"Table\"}]"
motivation: 土耳其语LVC因同形导致字面与习语意义区分困难，需评估监督学习与上下文学习在资源稀缺场景下的优劣。
method: 在手动构建的平衡数据集（含LVC正例、域外随机负例和域内字面控制负例）上，比较BERTurk与三种LLM的零样本、一样本和少样本提示分类性能。
result: 零样本LLM召回率极低；一样本提示大幅提升召回但引入强模型偏差；少样本提示改善校准，GPT-OSS-20B和Qwen 2.5-14B达到或超过监督基线。
conclusion: 提示工程对土耳其语元语言分类至关重要，少样本示例能有效提升LLM性能，监督基线在稳健性上仍有优势。
---

## 摘要
土耳其语惯用轻动词结构（LVC）对多词表达处理具有挑战性，因为它们通常与完全字面意义的动宾组合共享相同表面形式，同时作为部分惯用的单一谓词。我们将土耳其语LVC检测视为二分类任务（字面意义 vs. 惯用意义），并在人工创建的控制集（N=147）上评估，其中包含匹配的负例：领域外随机句子和领域内字面控制（NLVC），以及LVC正例。我们将有监督的土耳其语编码器基线（带分类头的BERTurk）与来自不同家族的三个指令微调LLM在零样本、单样本和少样本提示下进行比较，并分析演示如何改变错误分布。在零样本下，LLM在负例上表现良好，但LVC召回率非常低。单样本提示显著提高了LVC检测，但可能引入强烈的、模型特定的偏差，导致模型过度预测或欠预测LVC。更丰富的少样本提示改善了校准，并为GPT-OSS-20B和Qwen 2.5-14B带来了稳健的整体性能。总体而言，结果凸显了土耳其语元语言分类中显著的提示敏感性：有监督基线仍然具有竞争力，而提示LLM在精心构建的演示下可以在LVC上匹配或超越它。

## Abstract
Turkish idiomatic light verb constructions (LVCs) are challenging for multiword expression processing because they often share the same surface form as fully literal verb-object combinations while functioning as a single, partially idiomatic predicate. We frame Turkish LVC detection as a binary classification task (literal meaning vs. idiomatic meaning) and evaluate on a manually created controlled set (N=147) with matched negatives: out-of-domain random sentences and in-domain literal controls (NLVC), alongside LVC positives. We compare a supervised Turkish encoder baseline (BERTurk with a classifier head) to three instruction-tuned LLMs from different families under zero-shot, one-shot, and few-shot prompting, and analyze how demonstrations shift error profiles. In zero-shot, LLMs perform well on negatives but show very low LVC recall. One-shot prompting sharply improves LVC detection but can induce strong, model-specific biases, leading models to overpredict or underpredict LVCs. A richer few-shot prompt improves calibration and yields robust overall performance for GPT-OSS-20B and Qwen 2.5-14B. Overall, the results highlight substantial prompt sensitivity in Turkish metalinguistic classification: the supervised baseline remains competitive, while prompted LLMs can match or exceed it on LVCs with carefully constructed demonstrations.

---

## 论文详细总结（自动生成）

# 中文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：土耳其语中，惯用轻动词结构（LVC，如“ilham verdi”表示“启发”，字面是“给予灵感”）与完全字面义的动宾组合（如“kalem verdi”表示“给予一支笔”）共享相同表面形式，导致多词表达处理困难。如何精确区分这两种意义是NLP挑战。

- **研究动机**：多词表达（MWEs）普遍存在于日常语言，对NLP系统构成持久障碍。土耳其语形态丰富且LVC高度能产，字面/惯用界限模糊，尤其需要细粒度的语义判别能力。本研究旨在比较监督学习（BERTurk）与基于提示的上下文学习（instruction-tuned LLMs）在该任务上的表现，并探究提示工程对模型行为的影响。

- **整体含义**：提示敏感性显著，精心设计的少样本示例可使LLM匹配甚至超越监督基线，但零样本下LLM完全失败，说明当前LLM对低资源复杂形态语言的元语言判断能力不足，且依赖演示的校准方式。

## 2. 论文提出的方法论

### 核心思想
- 将土耳其语LVC检测视为**二分类任务**（标签[1]：包含LVC/惯用谓词；[0]：字面义谓词）。
- 在手动构建的**控制诊断集**上评估（N=147，三个平衡条件各49项：LVC正例、域内字面控制NLVC、域外随机负例Random），所有项目共享相同动词，以阻止动词本身作为leakage。
- 比较**监督基线**（BERTurk + 分类头，在土耳其语UD树库上微调）与**三种指令微调LLM**（Llama 3.1 8B、GPT-OSS-20B、Qwen 2.5 14B）在**零样本、单样本、少样本**三种提示机制下的表现。

### 关键技术细节
- **监督基线**：使用BERTurk（32K/128K两种词汇表），添加二分类头，在9个土耳其语UD树库（共82,319句，含9,491个LVC实例）上训练。训练超参：lr=2e-5, batch=32, weight decay=0.01, 10 epochs early stopping。
- **LLM提示**：所有LLM通过Ollama本地运行，temperature=0.1，单句处理。提示模板包含任务定义、标注规则、示例（单样本/少样本）。输出仅允许[0]或[1]。
- **少样本提示**：为每个目标动词提供一对正/负示例，共若干组。

### 公式/算法流程（文字说明）
- 整体实验流程：构建数据集 → 训练/配置模型 → 在三提示制下对147句分类 → 统计每条件成功率及错误类型（FP/FN） → 统计假设检验（卡方检验、Cramer's V、事后两比例检验，Holm校正） → 分析提示机制对决策边界的影响。

## 3. 实验设计

### 数据集
- **训练集**：9个土耳其语UD树库（ATIS、BOUN、FrameNet、GB、IMST、Kenet、PUD、Penn、Tourism），共82,884句原始句子，经自动提取和人工校验后保留82,319句（含9,491个LVC实例）。
- **评估集**：手动构建的**诊断集**（N=147），三个条件各49句：
  - LVC：惯用/轻动词结构正例。
  - NLVC：域内字面控制负例（使用相同动词）。
  - Random：域外随机负例（无关联动词）。
  所有句子经双人工校验，仅保留一致同意的项目。

### Benchmark / 对比方法
- **监督基线**：BERTurk-32k (clf)、BERTurk-128k (clf)。
- **LLM**：Llama 3.1 8B、GPT-OSS-20B、Qwen 2.5 14B，分别在：
  - 零样本（无示例）
  - 单样本（每动词一个正例、一个负例）
  - 少样本（多动词多个正负示例）
  条件下评估。

### 评估指标
- 各条件成功率（success rate），整体准确率，FP/FN计数，Precision/Recall（以LVC为正类）。

## 4. 资源与算力

- **监督基线**：未明确说明GPU型号、数量、训练时长。仅提到使用early stopping，最多10 epochs。
- **LLM实验**：所有LLM通过**Ollama**在本地运行，未指明使用哪种GPU或具体算力消耗。但提及模型为8B、14B、20B参数规模，量化版本为Q4_K_M（Llama/Qwen）和MXFP4（GPT-OSS），上下文长度分别为131k、32k等。
- **总体说明**：论文未提供详细的算力需求（如GPU小时、集群配置），这是一个信息缺口。

## 5. 实验数量与充分性

### 实验数量
- 共**三组主实验**（零样本、单样本、少样本），每组包含3个LLM模型+2个BERTurk基线（仅零样本条件下与BERTurk对比；监督基线不进行提示实验）。
- 统计检验：每个实验内对每个条件（Random/NLVC/LVC）进行卡方独立性检验，并在跨模型对比时进行Holm校正；同时进行了事后两比例检验。
- 还考察了**提示机制对模型的影响**（Regime × Correctness检验）以及**条件效应**（Condition × Model）。

### 充分性与客观性
- **优点**：使用控制诊断集而非大规模随机抽样，每个条件严格匹配动词，能精确揭示模型在字面/惯用边界上的决策行为。统计检验较为严谨（多重比较校正）。
- **不足**：
  - 评估集仅有147句，虽然作者强调“行为测试套件”的传统，但样本量较小，结论的泛化性有限。
  - 没有在更多样化的现实场景（如新闻、社交媒体、口语）上测试，缺乏外推验证。
  - 监督基线未在完全相同的评估集上监督训练（其训练标签来自UD树库标注，可能不同于本研究的标注约定），导致比较不完全公平。
  - 未进行消融实验（如不同数量演示、不同演示顺序、不同提示措辞）来分离影响。

## 6. 论文的主要结论与发现

1. **零样本下**：所有LLM在负例上表现优异（成功率>90%），但在LVC正例上近于失能（Llama 3.1 8B为0%，GPT-OSS 20B为6.1%，Qwen 2.5 14B为12.2%），呈现强保守偏见（倾向预测[0]）。监督基线BERTurk在LVC上达到67-80%成功率，显著优于LLM。跨模型差异主要由LVC条件驱动。

2. **单样本下**：添加一个正/负示例后，LVC召回率飙升（Llama达87.8%，GPT-OSS达83.7%），但引入极强烈的**模型特定偏见**：
   - Llama变为过度积极（高LVC召回但负例成功率暴跌，如NLVC仅28.6%）。
   - Qwen变得过度保守（负例近完美，但LVC仅49%）。
   - GPT-OSS最为均衡（LVC 83.7%，NLVC 73.5%）。
   所有条件下的模型差异均显著。

3. **少样本下**：进一步增加多动词示例后，校准明显改善：
   - GPT-OSS和Qwen整体准确率达到84-86%，接近监督基线（BERTurk 128k为86.4%）。
   - Llama仍然保守偏见（负例准确率仅51-61%），整体66.7%。
   - 跨模型差异在LVC条件上不显著（校正后p=0.075），但Random和NLVC上仍显著，提示模型在正类上趋于收敛，但负类校准仍有差距。

4. **提示机制效应**：对每个模型，提示机制（零/单/少样本）对LVC决策有极强影响（GPT-OSS和Qwen的卡方p值极高，Cramer's V >0.4）。这说明**演示主要改变决策边界而非提升理解**。

5. **整体启示**：对于形态丰富的土耳其语元语言分类任务，**提示敏感性极高**，需要精心构造示例；监督学习仍具竞争力且更稳定；少样本LLM可以匹配监督基线但依赖设计决策。

## 7. 优点

- **诊断性评估设计**：使用含匹配动词的控制集，可直接检验模型是否真正区分字面与惯用谓词，而非简单利用词汇频率或表面模式。
- **多层次对比**：同时比较监督学习与三种不同架构的LLM，覆盖零/单/少样本，揭示提示敏感性及模型特有偏差，分析全面。
- **严谨统计检验**：使用卡方检验、Cramer's V、事后两比例检验并修正多重比较，增强结论可靠性。
- **公开资源**：提供代码、提示模板、评估材料，促进可复现性。
- **实践洞察**：明确指出零样本LLM在土耳其语此类细粒度任务上的失效模式，为低资源、复杂形态语言的应用提供警示。

## 8. 不足与局限

- **评估集规模小**（N=147），仅涵盖有限动词模板，结论可能不泛化到其他土耳其语MWE类别（如固定习语、介词类MWE）或其他领域。
- **监督基线非完全直接比较**：BERTurk基于UD树库标签训练，其标注政策与本研究的LVC定义可能不一致，且训练与评估集来源不同，导致监督基线条件不一定最优。
- **LLM性能高度依赖提示设计**：单样本演示的选择、顺序、措辞都可能显著影响结果，本研究仅测试一种固定提示模板，未系统探索提示空间。
- **缺乏消融实验**：未分析不同演示数量、不同演示质量、不同输出格式的影响。
- **算力细节缺失**：未说明训练/推理的硬件配置、时间、能耗，不利于复现和成本估算。
- **仅聚焦土耳其语**：跨语言泛化性未知。

（完）
