---
title: Exploring Pre-training Benefits on Phoneme Addition through Fine-tuning in Speech Synthesis
title_zh: 通过微调探索预训练对语音合成中音素添加的益处
authors: "Masato Murata, Koichi Miyazaki, Tomoki Koriyama, Tomoki Toda"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.19792v1"
tags: ["query:low-res-tts"]
score: 9.0
evidence: 低资源文本转语音迁移学习
tldr: 在低资源语音合成中，当目标语料包含预训练未见音素时，模型需通过微调进行“音素增加”。研究通过LLM生成音素控制语料的模拟实验和英语到日语的跨语言真实语音实验，发现微调相比从头训练虽能提升自然度，但在新音素的音素错误率上需要相同甚至更多数据才能达到可比性能。结果表明预训练主要改善自然度，对音素增加帮助有限。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.19792v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 716, \"height\": 333, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19792v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 798, \"height\": 621, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19792v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 789, \"height\": 339, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.19792v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 830, \"height\": 332, \"label\": \"Table\"}]"
motivation: 预训练模型扩展音素库时是否受益于已有音素生成能力尚不明确，需探明其实际贡献。
method: 设计模拟（LLM生成音素控制语料）和真实跨语言（英→日）两种设置，对比微调与从头训练在新音素上的表现。
result: 微调自然度更高，但新音素PER与从头训练相当或更差，且所需数据量不减少。
conclusion: 预训练主要提升自然度，对音素增加的获益有限，需针对性设计新音素学习机制。
---

## 摘要
迁移学习广泛用于低资源文本到语音合成。当目标语料库包含预训练中未见过的音素时，模型必须在微调过程中扩展其音素库；我们将这一过程称为“音素添加”。然而，预训练生成已见音素的能力是否有助于这一过程仍不清楚。本研究在两种设置下探究音素添加：（1）使用大语言模型生成的音素控制语料库进行模拟，能够在无需考虑混杂因素的情况下进行研究；（2）真实语音的跨语言迁移设置（英语到日语），以验证发现是否在实践中成立。两种设置下的实验表明，虽然微调比从头训练实现了更高的自然度，但需要同样多甚至更多的数据才能在新音素上达到相当的PER。这些结果表明，预训练主要有助于自然度的提升，但对音素添加的益处有限。

## Abstract
Transfer learning is widely used for low-resource text-to-speech. When the target corpus contains phonemes unseen in pre-training, the model must expand its phoneme inventory during fine-tuning; we call the process "phoneme addition." However, it remains unclear whether the pre-trained ability to generate seen phonemes contributes to this process. This study investigates phoneme addition in two settings: (1) a simulation setup using LLM-generated phoneme-controlled corpora that enables investigation without considering confounding factors, and (2) a real-speech cross-lingual transfer setup (English to Japanese) to validate whether the findings hold in practice. Experiments in both settings showed that while fine-tuning achieved higher naturalness than training from scratch, it required as much or more data to achieve comparable PER for new phonemes. These results indicate that pre-training mainly contributes to naturalness improvement, but offers limited benefit for phoneme addition.

---

## 论文详细总结（自动生成）

# 论文结构化总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在低资源文本到语音合成（TTS）的迁移学习中，当目标语料包含预训练阶段未见过的音素（即“音素添加”，phoneme addition）时，预训练获得的“已见音素生成能力”是否真正有助于模型学习这些新音素？
- **背景**：
  - 迁移学习是低资源TTS的标准做法，通常基于高资源语言预训练，再在目标语言上微调。
  - 实际跨语言场景中，目标语言常有源语言中没有的音素（如英语→西班牙语需加颤音，英语→日语需加促音等），模型需扩展音素表。
  - 已有研究通过音素映射或随机初始化来处理未知音素，但仅报告了整体自然度和可懂度的提升，未明确验证预训练知识对音素添加过程的贡献。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：通过对比“微调（fine-tuning）”和“从头训练（scratch training）”在同一目标数据上的表现，分离出预训练知识对音素添加的实际影响。
- **关键技术细节**：
  - **模型架构**：Conformer-FastSpeech2 (CFS2)，来自ESPnet；波形生成使用预训练HiFi-GAN声码器（基于VCTK训练）。
  - **预训练**：在源数据集上训练多说话人CFS2（使用说话人ID条件代替x-vector），然后去除说话人条件变为单说话人模型进行微调。
  - **微调策略**（Naive Fine-tuning）：扩展音素表，新音素嵌入随机初始化；学习率降至原来1/10。
  - **模拟实验设置**：
    - 利用LLM（Claude Opus 4.6）生成“有限语料”（Limited Corpus，不含目标音素）用于预训练，和“全语料”（Full Corpus，含所有音素）用于微调。
    - 用基于VCTK训练的TTS模型合成语音，得到语音-音素对数据集。
    - 这样保证语言、说话人、数据领域固定，排除混杂因素。
  - **真实跨语言实验设置**：英语VCTK（预训练）→日语JSUT（微调），添加20个日语特有音素。

## 3. 实验设计：数据集、基准、对比方法

- **模拟实验**：
  - 源数据集：Limited dataset（107,000句，55小时，107说话人，排除目标音素）。
  - 目标数据集：Full dataset（2,000句，单说话人p299，包含所有40个英语音素）。
  - 目标音素类型：①爆破辅音（/p/,/b/,/t/,/d/,/k/,/g/）；②前元音（/i/,/ɪ/,/eɪ/,/ɛ/,/æ/）。
  - 对比方法：微调 vs. 从头训练（同一目标数据）。
  - 评估指标：Target PER（仅计算目标音素的音素错误率，用wav2vec 2.0音素识别模型）；UTMOS（预测MOS分，用于自然度）。
  - 数据量变化：100, 300, 500, 800, 1000, 2000句。
  - 测试集：VCTK文本子集1000句（含至少一个目标音素，未在训练集中出现过）。

- **真实跨语言实验**：
  - 源数据集：VCTK（44小时，108说话人）。
  - 目标数据集：JSUT basic5000（约10小时，单女性日语说话人）。
  - 目标音素：20个日语特有音素（如/a/,/e/,/o/, /b j/, /c/, /ts/, /tC/, /Q/等）。
  - 对比方法：微调（英语预训练→日语） vs. 日语从头训练。
  - 数据量变化：相同（100,300,...2000句）。
  - 测试集：JVS语料库100句（未在训练集中）。
  - 评估指标：Target PER（用基于JSUT fine-tuned的wav2vec 2.0模型）；UTMOS。

## 4. 资源与算力

- 论文**未明确说明**使用的GPU型号、数量、训练时长等具体算力信息。仅提及使用ESPnet实现，预训练基于VCTK，未提供硬件配置和训练时间。

## 5. 实验数量与充分性

- **实验数量**：
  - 模拟实验：2种目标音素类型 × 6种数据量 = 12组（每组包括微调和从头训练两个模型）。
  - 真实实验：1种多音素场景 × 6种数据量 = 6组。
  - 外加频谱分析（低资源条件下100句的对比）。
- **充分性评估**：
  - 覆盖了不同音素类型（辅音和元音）、不同数据量、两种完全不同的实验设置（模拟和真实），结论一致性高。
  - 使用了针对性的评估指标（Target PER）而非全局PER，更敏感。
  - 缺乏对更多音素类型（如声调、超音段特征）的验证；仅使用一种TTS架构（CFS2），未验证其他架构（如VITS、Tacotron）；真实实验只有英→日一种语言对。
  - 总体客观、公平，但代表性有一定局限。

## 6. 论文的主要结论与发现

- **主要结论**：
  1. 微调能成功学习新音素，但与从头训练相比，在目标音素的PER上需要**相同或更多**数据才能达到可比性能（即微调并无数据效率优势）。
  2. 在自然度（UTMOS）上，微调明显优于从头训练，尤其在低资源条件下。
  3. 预训练的“已见音素知识”对音素添加的帮助有限，主要贡献在于提升整体语音自然度。
- **解释**：从头训练可以无约束地同时学习所有音素，而微调需保留旧音素能力，导致新音素学习受干扰。

## 7. 优点

- **实验设计亮点**：
  - 使用LLM生成音素控制的合成语料，有效隔离语言、说话人、领域等混杂因素，是对音素添加过程进行纯净研究的重要创新。
  - 同时进行模拟和真实跨语言验证，增强了结论的可信度和泛化性。
  - 采用Target PER（受偏置WER启发）专门评估新音素，比整体PER更精确。
  - 频谱分析提供了直观的声学证据，支持结论。

## 8. 不足与局限

- **实验覆盖**：
  - 仅使用CFS2一种模型架构，未能验证结论是否适用于其他主流TTS模型（如VITS、Tacotron2）。
  - 真实跨语言实验仅一对语言（英→日），代表性不够，需更多语言对验证。
  - 目标音素类型有限（英语中仅两种，日语虽多但未对每类单独分析）。
- **微调策略**：
  - 仅使用了最简单的“naive fine-tuning”（新音素嵌入随机初始化），未探索其他可能更有效的策略（如特殊正则化、新音素加权损失、音素映射等）。
- **模拟实验局限**：
  - 依赖于LLM的质量和TTS模型合成质量，可能存在领域偏差。
  - 合成语音可能与真实语音分布不完全一致。
- **资源信息缺失**：未报告计算资源，不利于复现和评估成本。
- **潜在偏差**：未考虑源音素与目标音素之间的声学相似性对迁移效果的影响。

（完）
