---
title: Balancing ASR and diarization in end-to-end LLMs for multi-talker speech recognition
title_zh: 在端到端大语言模型中平衡ASR与说话人日志的多说话人语音识别
authors: "Naijun Zheng, Yuke Lin, Sanli Tian, Mengtian Li, Zhiwei Lin, Longshuai Xiao, Dandan Tu"
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.13095v1"
tags: ["query:speech-tech"]
score: 8.0
evidence: 基于LLM的多说话人语音识别，联合建模语义和说话人信息
tldr: "多说话人语音识别常用ASR与说话人日志流水线，LLM方法虽能联合建模但需大量标注数据。本文提出四种策略：双编码器提取语义与说话人特征、特征交错格式融合输入、长度感知损失增强日志、自适应阈值损失缓解重叠幻觉。在AliMeeting和Aishell4上分别相对提升18%和24%。该方法在有限真实数据下实现高性能，有效平衡ASR与日志任务。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.13095v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 809, \"height\": 744}, {\"url\": \"assets/figures/arxiv/2606.13095v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1595, \"height\": 244}, {\"url\": \"assets/figures/arxiv/2606.13095v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 828, \"height\": 430}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.13095v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 876, \"height\": 225}, {\"url\": \"assets/tables/arxiv/2606.13095v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1783, \"height\": 531}, {\"url\": \"assets/tables/arxiv/2606.13095v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 862, \"height\": 213}]"
motivation: 如何用有限真实数据高效训练LLM系统，准确分配说话人标签，避免大规模标注成本。
method: 提出双编码器架构、特征交错输入、长度感知说话人ID损失和自适应阈值ASR损失四种策略。
result: "在AliMeeting和Aishell4上分别获得18%和24%的相对改进。"
conclusion: 平衡ASR与说话人日志，在有限数据下实现高性能多说话人识别。
---

## 摘要
多说话人语音识别通常通过将自动语音识别（ASR）和说话人日志组合成流水线系统来解决。最近，基于大语言模型（LLM）的方法通过联合建模语义和说话人信息显示出前景，但它们通常需要大规模、注释成本高昂的多说话人语料库。本文研究了如何在有限真实录音数据下高效训练基于LLM的系统，同时保持说话人归属的高准确性。我们提出了几种策略：（1）双编码器架构以提取语义和说话人特征；（2）特征交错格式以将这些特征合并作为LLM的输入；（3）长度感知的说话人ID损失以增强日志能力；（4）自适应阈值策略用于ASR损失计算以减少语音重叠引起的幻觉。这些策略平衡了ASR和说话人日志任务之间的训练。我们的系统优于开源基线方法，在AliMeeting语料库和Aishell4语料库上分别实现了18%和24%的相对改进。

## Abstract
Multi-talker speech recognition is often addressed by combining automatic speech recognition (ASR) and speaker diarization in a pipeline system. Recently, LLM-based approaches have shown promise by jointly modeling semantic and speaker information, but they typically require large-scale multi-talker corpora that are costly to annotate. In this paper, we investigate how to efficiently train an LLM-based system with limited real-recorded data while maintaining high accuracy in speaker attribution. We propose several strategies: (1) a dual-encoder architecture to extract semantic and speaker features, (2) a feature interleaving format to merge these features as the inputs to the LLM, (3) a length-aware speaker ID loss to enhance diarization capability, and (4) an adaptive threshold strategy for ASR loss computation to mitigate hallucinations caused by speech overlaps. These strategies balance training between ASR and diarization tasks. Our system outperforms open-source baseline approaches, achieving relative improvements of 18% on the AliMeeting corpus and 24% on the Aishell4 corpus.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **任务**：多说话人语音识别，即“谁说了什么”。传统方法采用流水线（ASR + 说话人日志），模块独立训练，容易丢失语义与说话人信息的联合建模，尤其在语音重叠时。  
- **LLM 方法**：近年来利用大语言模型（LLM）同时建模语义和说话人角色，但通常需要大规模、带精细标注的多说话人语料（如数千小时会议数据），标注成本高昂。  
- **本文动机**：如何在仅有少量真实录音数据的情况下，高效训练 LLM 端到端系统，同时保持高精度的说话人归属。核心目标是 **平衡 ASR 与说话人日志任务**，避免对大规模数据的依赖。

## 2. 方法论：核心思想、关键技术细节

### 2.1 整体架构
- **双编码器结构**  
  - **ASR 编码器**：采用 SenseVoice-small（CTC 模块），提取语义特征（隐层状态拼接后经适配器降维）。  
  - **说话人编码器**：采用 Campplus（修改为分块统计，块大小 400ms/200ms/100ms，拼接后经卷积下采样对齐长度）。编码器参数冻结。  
- **特征融合策略**（关键创新之一）  
  - 对比了三种融合方式：**特征维度拼接**（损伤表示）、**时间维度拼接**（参考 TagSpeech，需插入数字 token）、**时间交错**（每 N 帧交替插入语义块和说话人块，靠位置编码对齐）。实验证明 **时间交错** 最有效，与标签结构更匹配。

### 2.2 训练流程
- **多阶段训练**（三阶段 + 微调）  
  - 阶段一：仅语义特征，ASR 任务训练（适配器 + LoRA 微调 LLM）。  
  - 阶段二：引入说话人特征，使用内部双人对话数据（约 4000 小时），标签格式为 `{文本1 <SC> spk1} {文本2 <SC> spk2}`。  
  - 阶段三：拼接多个片段模拟长对话（最多 8 人），虚拟 `<SC>` 标记用于同一说话人停顿，避免误报导致 ID 错误。  
  - 最后阶段：在真实会议语料（AliMeeting、Aishell4）上微调，使用自适应的损失掩码。

### 2.3 关键损失函数
- **长度感知说话人 ID 损失**（`L_spk`）  
  - 对每个说话人段按长度加权计算交叉熵，权重大小与段长度成正比，更好地对齐 cpCER 指标。  
- **自适应 ASR 损失掩码**（缓解重叠幻觉）  
  - 观察到重叠区域的 ASR 损失极高，模型倾向于重复生成填充词（如“嗯”“对”）。  
  - 提出动态阈值：\( T_{mask} = \max(\text{Avg}(CE(\text{text})), 2.0) \)，训练时屏蔽超过阈值的损失。总损失为 \( L = L_{spk} + \text{Mask}(L_{ASR}, T_{mask}) \)。

## 3. 实验设计

- **数据集**  
  - 预训练：WenetSpeech（ASR 基础）。  
  - 阶段二/三：内部 4000 小时双人对话（无重叠标注），拼接模拟。  
  - 最终微调/评估：**AliMeeting**（重叠率 18.80%–25.82%）和 **Aishell4**（重叠率 5%–7.18%），仅使用远场第一通道。  
- **基准方法**  
  - 流水线：Paraformer-large + 3D-Speaker / Diarizen-large。  
  - 端到端：VibeVoice-ASR（7B）、SpeakerLM（7B，分别在 212h/7638h 训练）。  
  - 自身消融：不同融合策略、损失配置、下采样率等。  
- **评估指标**：CER（字符错误率）、cpCER（带说话人分配的 CER）、Δcp = cpCER – CER。

## 4. 资源与算力

- 论文 **未明确指出** GPU 型号、数量或训练时长。  
- 仅提及：模型规模 ~0.7B（Qwen2.5-0.5B-Instruct 为后端），LoRA rank=16，学习率 1e-4（前三阶段）→2e-5（最后阶段），warm-up 0.03。  
- 未公开训练耗时，读者需自行推测（0.7B 模型规模较小，预计单卡或少量卡可训练）。

## 5. 实验数量与充分性

- **充分性**：  
  - 覆盖 AliMeeting（Eval + Test）和 Aishell4（Eval）共 3 个测试集。  
  - 与两种流水线、两种端到端 LLM 基线对比。  
  - 进行了 **特征融合消融**（4 种策略）、**损失函数消融**（移除说话人损失 / 不同阈值 / 无 ASR 损失等）、**下采样消融**（不同 I/O 比）、**数据重对齐消融**。  
  - 实验结果表格清晰，统计了绝对值与相对提升。  
- **客观性**：  
  - 承认与 SpeakerLM 测试集不一致，但使用相同基线（Paraformer+3D-Speaker）对比，显示了困难程度。  
  - 消融实验证实各组件均有贡献。  
  - 未过度调参，学习率、LoRA rank 等常见设置。  
- **不足**：未在多说话人数量变化、不同语言、远场噪声等更广泛场景下测试；未与最近其他端到端 LLM 方法（如 MOSS Transcribe）对比（可能因时间差）。

## 6. 主要结论与发现

- **时间交错融合** 优于特征维度拼接和时间维度拼接，最好地保留了语义与说话人信息的对齐。  
- **自适应 ASR 损失掩码** 有效减少重叠区域产生的重复/填充幻觉，在 AliMeeting Test 上 cpCER 相对下降 8.5%。  
- **长度感知说话人 ID 损失** 提升了说话人辨识准确性。  
- 最终系统（0.7B 参数）在 AliMeeting 和 Aishell4 上分别比流水线基线相对提升 18% 和 24%（cpCER 指标），且性能接近用 7638 小时数据训练的 SpeakerLM（7B）。  
- 适当下采样说话人特征（如保留 25%）可平衡效率与性能，甚至仅保留 10% 仍比纯语义特征有明显提升。

## 7. 优点

- **数据高效**：仅用有限真实会议数据（约 100 小时 AliMeeting + 107 小时 Aishell4）和内部模拟数据，达到甚至超越大规模训练的模型。  
- **结构创新**：双编码器 + 时间交错融合，有效分离并联合语义与声学特征。  
- **损失设计巧妙**：长度感知 + 自适应掩码，直接针对评估指标（cpCER）和重叠问题。  
- **消融充分**：验证了每个模块的必要性，为后续研究提供可靠指导。  
- **可扩展性**：0.7B 模型轻量，容易部署或进一步放大。

## 8. 不足与局限

- **算力信息缺失**：未给出 GPU 型号、数量、训练时间，不利于复现和资源评估。  
- **实验覆盖有限**：仅测试中文会议数据集（AliMeeting、Aishell4），未涉及英语、多语种或更复杂场景（如远场强噪声、多人同时说话超过 8 人）。  
- **标签假设较强**：需要精确的说话人段标注和 `<SC>` 标记，实际应用中若标注不精确可能影响性能。  
- **幻觉缓解不完全**：仅屏蔽高损失 token，但未从根本上解决重叠语音的不可辨识性，可能仍存在残留幻觉。  
- **未对比近期其他端到端方法**：如 MOSS Transcribe、TagSpeech 等（可能因时间差，最新版本在论文提交时尚未公开）。  

（完）
