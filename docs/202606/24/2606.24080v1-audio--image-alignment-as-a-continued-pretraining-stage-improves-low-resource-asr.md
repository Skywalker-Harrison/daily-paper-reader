---
title: Audio--Image Alignment as a Continued-Pretraining Stage Improves Low-Resource ASR
title_zh: 音频-图像对齐作为持续预训练阶段改善低资源自动语音识别
authors: "Sujith Pulikodan, Nihar Desai, Prasanta Kumar Ghosh"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24080v1"
tags: ["query:speech-tech"]
score: 9.0
evidence: 利用音频-图像对齐的低资源语音识别
tldr: 低资源语言ASR因缺乏转录数据而受限。本文提出在预训练与监督微调间插入表示对齐阶段，利用Vaani数据集的音频-图像对，将图像表示与音频表示对齐，无需转录数据即可适应音频编码器。多个视觉编码器实验表明，对齐后微调的模型在低资源ASR上始终优于直接微调。该工作展示了跨模态对齐作为低成本适应策略的潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24080v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 702, \"height\": 748, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24080v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1718, \"height\": 1994, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24080v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 848, \"height\": 479, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24080v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1207, \"height\": 391, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24080v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1303, \"height\": 784, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24080v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1366, \"height\": 1005, \"label\": \"Table\"}]"
motivation: 低资源语言ASR缺乏高质量转录数据，需探索无需转录的跨模态适应方法。
method: 在预训练FastConformer音频编码器后，使用Vaani数据集将图像表示与音频表示对齐，再监督微调。
result: 对齐后微调的模型在低资源ASR上始终优于直接微调，验证了不同视觉编码器的有效性。
conclusion: 音频-图像表示对齐是一种有效的转录免费适应策略，可提升低资源ASR性能。
---

## 摘要
全世界有数千种语言，但由于高质量的转录语音数据有限，其中许多语言在自动语音识别（ASR）方面仍然资源不足。收集准确的转录通常成本高昂且劳动密集，尤其是对于低资源语言。在这项工作中，我们研究了使用对齐的音频-图像对来适配预训练的音频编码器，而无需在监督微调之前使用转录数据。我们提出的表示对齐阶段被引入到大规模预训练和监督ASR微调之间。具体来说，从预训练视觉编码器中提取的图像表示与音频表示对齐，以进一步适配预训练的音频编码器。对于这个对齐过程，我们使用了Vaani数据集，其中图像作为语音收集的提示，自然提供了配对的音频-图像数据。我们使用多个视觉编码器和预训练的FastConformer音频编码器评估了所提出的方法。实验结果表明，与直接微调相比，在表示对齐后微调的模型始终实现了更好的ASR性能。这些发现突显了音频-图像表示对齐作为一种有效的无转录适配策略的潜力，可用于增强低资源语言环境中的ASR系统。

## Abstract
Thousands of languages are spoken worldwide, yet many remain under-resourced for Automatic Speech Recognition (ASR) due to the limited availability of high-quality transcribed speech data. Collecting accurate transcriptions is often costly and labor-intensive, particularly for low-resource languages. In this work, we investigate the use of aligned audio-image pairs to adapt pretrained audio encoders without requiring transcription data before supervised fine-tuning. Our proposed representation alignment stage is introduced between large-scale pretraining and supervised ASR fine-tuning. Specifically, image representations extracted from pretrained vision encoders are aligned with audio representations to further adapt a pretrained audio encoder. For this alignment process, we utilize the Vaani dataset, in which images serve as prompts for speech collection, naturally providing paired audio-image data. We evaluate the proposed approach using multiple vision encoders and a pretrained FastConformer audio encoder. Experimental results demonstrate that models fine-tuned after representation alignment consistently achieve improved ASR performance compared to direct fine-tuning. These findings highlight the potential of audio-image representation alignment as an effective transcription-free adaptation strategy for enhancing ASR systems in low-resource language settings.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：全球数千种语言中，大部分语言的自动语音识别（ASR）因缺乏高质量转录语音数据而资源不足。传统标注成本高、耗时长，尤其在低资源语言场景下，探索无需转录的语音表示适配方法至关重要。
- **整体含义**：本文提出在语音预训练与监督微调之间插入一个**音频-图像表示对齐阶段**，利用自然收集的语义对齐音频-图像对来进一步优化音频编码器，无需任何文本标注。这种跨模态无转录适应策略能有效提升低资源ASR性能，特别是对域外数据的泛化能力。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：三阶段流水线：大规模自监督音频预训练 → 音频-图像表示对齐（无转录） → 监督ASR微调（有转录）。对齐阶段使用对比学习将音频表示与来自预训练视觉编码器的图像表示对齐，音频编码器参数全量更新。
- **关键技术细节**：
  - **音频编码器**：17层FastConformer，预训练于Vaani数据集（全部约31k小时，但不包含评测音频）。
  - **视觉编码器**：实验三种：SigLIP2 Base（单token池化，768维）、SigLIP2 Large（16 token，1024维）、Qwen3-VL（16 token，2048维）。均冻结，图像嵌入预计算缓存。
  - **对齐头**：MLP将音频表示投影到图像嵌入空间。SigLIP2 Base使用单查询注意力池化后余弦相似度；SigLIP2 Large和Qwen-VL使用多查询注意力池化（Ka=16查询）与图像token的MaxSim分数（对每个音频查询取最大余弦相似度后平均）。
  - **损失函数**：SigLIP sigmoid对比损失（可学习温度`t`和偏置`b`），使用跨GPU的batch内负样本，`−log σ(y_ij (t·s_ij + b))`。
  - **优化**：AdamW (β1=0.9, β2=0.95, weight decay=0.01, gradient clip=1.0, bfloat16)。新初始化的层学习率3e-4，预训练编码器学习率缩放0.05倍。1000步warmup后衰减至0.05倍。所有对齐配置训练200k步。

### 3. 实验设计：数据集、场景、基准、对比方法

- **数据集**：
  - **Vaani**：约31,255小时语音（105语言），其中1,894小时有转录。采用“图片提示”采集范式，自然产生对齐音频-图像对。用于预训练、对齐（11,848,593对，16,580小时）和微调（1,894小时转录）。评测集包含48语言（81小时）。
  - **FLEURS**：南亚14种第八附表语言，用于跨域泛化评估（训练124.35小时，测试36.88小时）。
- **基准（Baseline）**：使用相同预训练FastConformer编码器，跳过对齐阶段，直接进行监督微调（使用相同解码器、微调配置）。
- **对比方法**：三种对齐变体（SigLIP2 Base、SigLIP2 Large、Qwen3-VL），分别微调后与基准比较WER。
- **统计显著性**：配对句子级bootstrap测试（2000次重采样），报告95%置信区间和p值。

### 4. 资源与算力

- **微调阶段**：8×NVIDIA H100 GPU，batch size=16 per GPU，AdamW Noam调度，学习率1e-4，30 epochs。
- **对齐阶段**：batch size=64，AdamW优化器，200k训练步（具体GPU数量未明确，但文中提及DDP gather across GPUs，推测使用多GPU，可能类似微调的8 GPU或更多）。
- **预训练阶段**：未说明具体GPU数量和时长，仅提及从零开始在Vaani上预训练FastConformer。

### 5. 实验数量与充分性

- **主要实验**：两组微调场景（Vaani 48语言 + FLEURS 14语言）× 三种对齐变体 + 一个基线，共8个主要模型。
- **消融实验**：
  - 不同微调数据量（10/20/30/40/50小时）下基线 vs 对齐模型（图3），验证低资源增益更显著。
  - 检查是否仅是额外音频预训练造成提升：比较对齐前三个连续checkpoint（WER分别为48.70, 48.98, 48.75），证明对齐本身贡献。
- **统计检验**：对每个语言进行配对bootstrap检验，区分显著改善/退化（p<10^-4），结果表格详细。
- **充分性评价**：实验设计合理，覆盖域内（Vaani）和域外（FLEURS）评估，数据量级足够，有消融和统计检验。但仅使用一种音频编码器（FastConformer）和固定预训练数据（Vaani），未探索其他SSL模型或更大规模语言集（如非洲语言），通用性待验证。

### 6. 论文的主要结论与发现

- **主要结论**：音频-图像表示对齐作为持续预训练阶段能够持续降低WER，且对低资源语言（<50小时转录）收益明显；在域外FLEURS上WER从0.6778降至0.5338（SigLIP2 Base），相对降低21.26%。
- **关键发现**：
  - SigLIP2 Large在Vaani上最佳（-2.47%），SigLIP2 Base在FLEURS上最佳（-21.26%）。
  - 对齐阶段带来的改进并非来自额外音频预训练，而是跨模态对齐信号。
  - 随着微调数据量增加，对齐的增益逐渐减小，凸显其在低资源场景下的价值。
  - 对齐主要提升泛化能力而非域内拟合（Vaani提升较小，FLEURS提升很大）。

### 7. 优点

- **新颖性**：在预训练和微调之间插入无转录的跨模态对齐阶段，利用易获取的音频-图像对替代文本标注，思路简洁实用。
- **方法扎实**：采用对比学习、多查询注意力池化、MaxSim分数等成熟技术，适配三种不同视觉编码器，并报告统计显著性。
- **实验严谨**：控制了“额外音频预训练”这一混淆因素，进行消融和bootstrap检验，验证对齐的因果关系。
- **实际价值**：证明在低资源ASR中可显著降低成本（无需转录即可提升），对多语言语音技术有直接应用潜力。
- **开源友好**：使用公开Vaani数据集，基于NeMo框架，可复现性高。

### 8. 不足与局限

- **实验覆盖有限**：仅测试了Vaani（特定采集范式）和FLEURS（南亚14语言），未涉及非洲、东南亚等更多低资源语言；仅使用FastConformer一种音频编码器，未在wav2vec 2.0/HuBERT上验证。
- **对齐数据本身包含音频**：对齐使用的音频已出现在预训练集中（无新音频），但图像信息是新的。若图像也出现在预训练中（但视觉编码器冻结），可能增益部分来自视觉模态的补全。这一点未深入分析。
- **计算开销**：对齐阶段需要额外200k步训练及视觉编码器推理缓存，对于低资源团队可能仍是负担。
- **未探讨失败案例**：部分语言（如Vaani中14种语言退化，FLEURS中Urdu无改善）未给出解释，可能因语言特性或对齐质量不佳。
- **理论分析欠缺**：未量化对齐后音频表示与图像语义的对齐程度（如检索精度），仅靠ASR性能间接说明，缺少机理探讨。
- **微调数据量上限低**：仅在最多50小时转录上验证趋势，更高资源场景（上百小时）未覆盖。

（完）
