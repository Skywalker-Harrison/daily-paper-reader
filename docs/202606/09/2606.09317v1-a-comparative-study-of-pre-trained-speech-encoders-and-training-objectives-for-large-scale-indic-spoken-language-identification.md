---
title: A Comparative Study of Pre-trained Speech Encoders and Training Objectives for Large-Scale Indic Spoken Language Identification
title_zh: 大规模印度语种口语识别中预训练语音编码器和训练目标的比较研究
authors: "Agneedh Basu, Pavan Kumar J, Sujith P, Visruth Sanka, Nihar Desai, Prasanta Kumar Ghosh"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.09317v1"
tags: ["query:speech-tech"]
score: 8.0
evidence: 42种印度语言口语识别中语音编码器的比较研究
tldr: "印度语言种类多、重叠严重且标注稀缺，语言识别挑战大。本文系统对比Whisper和FastConformer两种预训练编码器及交叉熵、对比学习、分层Softmax三种训练目标在42种印度语种上的表现。冻结FastConformer在跨域测试集上宏准确率超90%；分层Softmax一致最优，尤其提升跨域泛化；对比学习反而损害泛化。分析发现印欧语系中部印度-雅利安语支最难区分，印地语-乌尔都语等混淆最严重。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09317v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 929, \"height\": 1597, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.09317v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 908, \"height\": 249, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09317v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 924, \"height\": 397, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09317v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 833, \"height\": 411, \"label\": \"Table\"}]"
motivation: 印度语言数量多、语音相似度高且标注稀缺，现有LID方法缺乏对大规模印度语种下预训练编码器和训练目标的系统比较。
method: 采用Whisper和FastConformer两种预训练编码器，结合线性分类器，在Vaani数据集上训练并比较CE、CE+SupCon、HSM三种目标。
result: "冻结FastConformer在FLEURS和Kathbath上超过90%宏准确率，优于Whisper；HSM在所有基准上一致优于其他目标；CE+SupCon损害FastConformer的跨域泛化。"
conclusion: 分层Softmax是印度语言识别的最优训练目标；冻结FastConformer跨域泛化强；对比学习可能导致域内过拟合，需谨慎使用。
---

## 摘要
印度语言的口语语种识别（LID）是一项具有挑战性的问题，原因在于语言数量众多、相关变体之间存在显著的语音重叠，以及许多低资源语言的标注数据稀缺。在本研究中，我们对两种预训练语音编码器——Whisper和FastConformer——结合线性分类器进行了系统的比较研究，用于覆盖四个语系的42种语言的大规模印度语种识别。我们在冻结（线性探测）和微调设置下评估了两种编码器，并比较了三种训练目标：交叉熵（CE）、有监督对比损失结合交叉熵（CE + supCon）以及层次化Softmax（HSM）。模型在Vaani数据集上训练，并在Vaani-Test（保留集）、FLEURS和Kathbath上进行跨语料库评估，从而提供了领域泛化的见解。冻结的FastConformer编码器在FLEURS和Kathbath上实现了超过90%的宏平均准确率，且无需任何任务特定适应，在域外基准上显著优于Whisper，而微调的Whisper在域内性能上表现更强。HSM在所有基准上均一致优于CE和CE+SupCon，且在域外测试集上提升最大。CE+SupCon降低了FastConformer的跨语料库泛化能力，表明对比目标使表示过度专用于域内条件。按语系分析显示，中央印度-雅利安语支最难区分，其中印地语-乌尔都语以及萨德里语-恰蒂斯加尔语-苏尔古贾语聚簇是主要的混淆对。

## Abstract
Spoken language identification (LID) for Indian languages is a challenging problem due to the large number of languages, significant phonetic overlap among related varieties, and the scarcity of labeled data for many low-resource languages. In this work, we present a systematic comparative study of two pre-trained speech encoders -- Whisper and FastConformer -- combined with a linear classifier for large-scale Indic LID spanning 42 languages across four linguistic families. We evaluate both encoders in frozen (linear probing) and fine-tuned settings, and compare three training objectives: cross-entropy (CE), supervised contrastive loss with cross entropy (CE + supCon), and hierarchical softmax (HSM). Models are trained on the Vaani dataset and evaluated in a cross-corpus setting on Vaani-Test (held-out), FLEURS, and Kathbath, providing insights into domain generalization. The frozen FastConformer encoder achieves over 90\% macro accuracy on FLEURS and Kathbath without any task-specific adaptation, substantially outperforming Whisper on out-of-domain benchmarks, while fine-tuned Whisper yields stronger in-domain performance. HSM consistently outperforms CE and CE+SupCon for both encoders across all benchmarks, with the largest gains on out-of-domain test sets. CE+SupCon degrades FastConformer's cross-corpus generalization, suggesting that the contrastive objective over-specializes representations to in-domain conditions. Per-family analysis shows that Central Indo-Aryan varieties are the hardest to discriminate, with Hindi--Urdu and the Sadri--Chhattisgarhi--Surgujia cluster being the dominant confusion pairs.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

印度拥有超过1600种母语，其中22种为宪法承认语言，分属印度-雅利安、达罗毗荼、汉藏和欧洲四个语系。自动口语语种识别（LID）是多语言语音处理流程的关键前端，但面临三大挑战：相关语言变体（如印地语、博杰普尔语、恰蒂斯加尔语等）语音高度重叠；许多目标语言为低资源语言，标注数据稀缺；现有基准在录音条件、说话风格等方面差异大，导致跨语料库泛化困难。现有工作在语言覆盖度（通常≤7种）和跨域评估上存在局限。本文旨在通过系统性比较两种主流预训练语音编码器（Whisper和FastConformer）和三种训练目标（交叉熵、对比学习、分层Softmax），确定最适合大规模印度语种LID的架构与目标设置。

### 2. 论文提出的方法论：核心思想、关键技术细节

**核心思想**：采用“预训练编码器 + 线性分类头”的简单架构，分别探索冻结（线性探测）和微调两种模式，并比较不同训练目标对嵌入空间和分类性能的影响。

**关键技术细节**：
- **编码器**：
  - **Whisper**（medium变体，350M参数，输出1024维帧级表示）：基于ASR弱监督训练，覆盖90+种语言。
  - **FastConformer**（430M参数，输出1024维）：专门在60+种印度语言上预训练，具有更强的域内表示能力。
- **池化**：使用自注意力池化（Self-Attention Pooling）将帧级序列压缩为固定长度话语嵌入z。
- **分类头**：单个线性层，将z映射到L=42个语言的对数几率。

**训练目标**：
1. **交叉熵（CE）**：标准多类交叉熵损失。
   \[
   L_{CE} = -\sum_{l=1}^{L} y_l \log \hat{p}_l
   \]
2. **有监督对比损失+交叉熵（CE+SupCon）**：
   \[
   L_{CE+SupCon} = \lambda L_{SupCon} + (1-\lambda) L_{CE}
   \]
   其中SupCon损失在同一批次中拉近同语言样本嵌入、推开不同语言样本，温度τ=0.07，权重λ=0.5。
3. **分层Softmax（HSM）**：将42种语言组织为四层语言学树（根→语系→亚语系→语言），每个内部节点有一个线性分类器预测其子节点概率，最终语言概率为路径上各节点条件概率的乘积。损失为路径上各节点交叉熵之和。这利用了语言学先验知识，减少输出空间复杂度。

**训练流程**：在冻结设置下仅训练最后两层（池化+分类头）；微调设置下更新整个编码器权重。

### 3. 实验设计

- **数据集**：
  - **训练**：Vaani语料库中精选42种语言，每语言10小时，按8:1:1划分为训练/验证/测试（说话人不重叠）。
  - **测试**：
    - Vaani-Test（保留集，42种语言，域内）
    - FLEURS（13种语言，域外）
    - Kathbath（11种语言，域外）
- **基准方法**：
  - 内部：两种编码器（Whisper、FastConformer）× 两种设置（冻结、微调）× 三种目标（CE、CE+SupCon、HSM）
  - 外部：Facebook MMS（仅支持30种目标语言）、SpeechBrain ECAPA-TDNN（仅13种）
- **评估指标**：宏平均准确率（macro accuracy），每个语言平等对待。
- **对比维度**：编码器架构、是否微调、训练目标、跨域泛化。

### 4. 资源与算力

文中明确提到：
- 所有实验在一块 **NVIDIA L40 GPU** 上运行。
- 优化器：AdamW，线性学习率预热。
- 批量大小：4（有效批量96，通过梯度累积24步实现）。
- 学习率：微调1e-5，线性探测1e-4。
- **未说明**：训练总时长、每个实验的轮数（epoch数）以及重复实验次数。因此无法评估计算开销的精确量级。

### 5. 实验数量与充分性

- **主要实验**：
  - Table I：冻结 vs. 微调（CE损失）→ 2×2=4组结果。
  - Table II：微调下三种目标 + 两个外部基线 → 2×3+2=8组结果（但外部基线语言覆盖不全，仅部分可比）。
  - Table III：按语系性能分析（HSM损失，两种编码器）→ 2×（多个语系子项）。
  - Figure 1：中央印度-雅利安语支混淆矩阵（两种编码器）。
- **充分性评价**：
  - **优点**：覆盖了两种主流编码器、三种代表性训练目标、冻结/微调、域内/域外多基准，且进行了按语系细粒度分析。实验设计系统、控制变量清晰。
  - **不足**：
    - 未对CE+SupCon中的λ和τ进行消融（仅用固定值0.5和0.07），可能不是最优。
    - 未报告多次实验的均值与标准差，无法评估结果的统计稳定性。
    - 缺少对低资源语言的单独分析（如每种语言的准确率）。
    - 外部基线因语言覆盖不全，可比性有限（Vaani-Test得分偏差大）。
    - 未对比其他更先进的LID方法（如ECAPA-TDNN经域适应后的版本）。

### 6. 论文的主要结论与发现

1. **编码器对比**：冻结FastConformer在FLEURS和Kathbath上宏准确率>90%（94.2%/90.9%），远高于冻结Whisper（61.9%/57.7%）；即使微调后，FastConformer+HSM（91.4%/90.0%）仍优于最佳Whisper结果（73.8%/75.8%）。表明FastConformer预训练产生了更鲁棒的跨域表示。
2. **微调影响**：Whisper从微调中获益显著（域内提升15.8%）；FastConformer微调仅小幅提升域内（0.2%），却降低域外性能（FLEURS下降4.3%），说明其冻结表示泛化性更好。
3. **训练目标**：HSM在所有基准上一致最优，尤其是Kathbath（Whisper提升7.5%，FastConformer提升2.6%）。CE+SupCon反而损害FastConformer的跨域泛化（FLEURS下降7.2%），表明对比学习可能造成域内过拟合。
4. **语系难度**：中央印度-雅利安语支最难（58.7%），主要混淆对为印地语-乌尔都语以及萨德里-恰蒂斯加尔-苏尔古贾聚簇。汉藏语系和达罗毗荼语系识别准确率较高（>85%）。

### 7. 优点

- **全面性**：首次在42种印度语言的大规模设定下系统比较两种主流预训练编码器和三种训练目标，且同时考虑了冻结/微调。
- **跨域评估**：使用三个差异显著的测试集（Vaani-Test、FLEURS、Kathbath），提供了域内与域外性能的清晰对比，更贴近实际部署。
- **语言学先验利用**：HSM将语言学层次结构融入损失函数，不仅提升准确率，还提供了可解释的误差分析（按语系/亚语系）。
- **公平性**：对比实验中保持编码器和分类头结构一致，仅改变训练目标或微调设置，控制变量严格。

### 8. 不足与局限

- **实验复现性**：未报告多次实验的平均值和标准差，难以判断结果是否显著；训练轮次和收敛标准未说明。
- **超参数调优**：CE+SupCon的λ和τ为固定值，未进行网格搜索或其他调优，可能低估了其潜力或导致结论偏差。
- **低资源语言评估**：未单独报告每种语言的准确率，尤其对中央印度-雅利安语支中如Halbi、Bajjika等语言的表现缺乏细粒度分析。
- **分类头简单**：仅使用线性层，未探索更复杂的分类器（如MLP、注意力池化后再分类），可能限制了编码器表示的下游利用。
- **外部基线可比性**：MMS和ECAPA-TDNN仅覆盖部分语言，Vaani-Test得分无法公平比较（MMS仅30/42语言，得分33.7%可能因未覆盖语言而偏低）。
- **未考虑数据增强**：对于语音重叠严重的语言，未尝试语音扰动、速度调整等数据增强方法来改善混淆。
- **泛化性边界**：结论仅基于Vaani训练集，是否适用于其他印度语料库（如IIIT-H、LDC等）尚待验证。

（完）
