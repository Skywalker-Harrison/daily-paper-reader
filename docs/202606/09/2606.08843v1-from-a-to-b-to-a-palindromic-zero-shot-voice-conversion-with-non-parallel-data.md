---
title: "From A to B to A: Palindromic Zero-Shot Voice Conversion with Non-Parallel Data"
title_zh: 从A到B再到A：基于非平行数据的回文零样本语音转换
authors: "Moshe Mandel, Shlomo E. Chazan"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08843v1"
tags: ["query:speech-tech"]
score: 8.0
evidence: 支持多语言非平行数据的零样本语音转换框架
tldr: 语音转换通常需要平行数据或显式对齐。本文提出基于KNN检索WavLM表示的非平行数据对齐方法，构建合成到真实的训练范式，并加入说话人损失确保身份一致性。在英语训练下，该方法在多个语言上实现了高自然度和说话人相似度，优于竞争基线。关键贡献是无需平行语料即可实现高质量零样本语音转换。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08843v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 793, \"height\": 729, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08843v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1709, \"height\": 547, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08843v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 690, \"height\": 355, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08843v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1767, \"height\": 787, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08843v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 838, \"height\": 343, \"label\": \"Table\"}]"
motivation: 现有语音转换依赖平行数据或显式对齐，难以扩展到多语言和零样本场景。
method: 利用KNN检索WavLM特征对齐源目标语音，构造合成训练对，并引入说话人损失保持身份。
result: 仅用英语数据训练，在多语言测试中自然度和相似度均优于基线，支持零样本转换。
conclusion: 提出无需平行语料的语音转换框架，通过检索对齐和说话人损失实现高质量跨语言零样本转换。
---

## 摘要
我们提出了一种语音转换（VC）框架，该框架利用对WavLM表示的K近邻（KNN）检索来对齐非平行的源语音和目标语音，从而为监督学习构建合成训练对。检索到的片段作为合成输入，而真实的目标音频作为真实输出，形成一种合成到真实的训练范式，该范式天然支持多语言数据，无需平行语料库或显式对齐。为了确保目标说话人身份的一致性，我们引入了一个源自预训练说话人验证模型的说话人损失。跨多种语言的实验表明，尽管仅在英语数据上训练，该提出的方法仍实现了高自然度和强说话人相似性，优于具有竞争力的VC基线。样本可访问：https://palindromic-vc.github.io。

## Abstract
We present a voice conversion (VC) framework that utilizes K-Nearest Neighbors (KNN) retrieval over WavLM representations to align non-parallel source and target speech, constructing synthetic training pairs for supervised learning. The retrieved segments serve as synthetic inputs, while real target audio provides ground-truth outputs, forming a synthetic-to-real training paradigm that naturally supports multilingual data without requiring parallel corpora or explicit alignment. To ensure consistent target-speaker identity, we incorporate a speaker loss derived from a pretrained speaker verification model. Experiments across multiple languages demonstrate that the proposed approach achieves high naturalness and strong speaker similarity, outperforming competitive VC baselines, despite being trained exclusively on English data. Samples can be accessed at: https://palindromic-vc.github.io.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：传统语音转换（VC）系统严重依赖平行语料（即同一内容由不同说话人录制），但收集大规模平行语料成本高、不现实。非平行方法通常通过解耦内容与说话人特征来实现，但存在内容泄露、说话人信息保留不完善、依赖ASR/TTS等语言相关模型等问题，且难以扩展到零样本（unseen说话人）和任意对任意（any-to-any）的场景。
- **核心问题**：如何在不使用平行数据、不依赖语言模型（如ASR/TTS）的前提下，实现高质量、多语言、零样本的语音转换。
- **整体含义**：本文提出了一种“回文”（palindromic）训练框架，通过KNN检索WavLM特征合成源-目标对，将非平行数据转化为监督信号，结合说话人验证损失，仅用英语数据训练就实现了多语言泛化，在说话人相似性和等错误率（EER）上超越多种竞争方法。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：
  - 利用预训练的WavLM模型提取帧级特征。
  - 在训练时，对同一目标说话人的一段语音，通过KNN-VC（K近邻语音转换）方法，将其特征与库中任意参考说话人的特征进行帧级匹配，生成“合成源特征”（记为Ŝ1），以真实目标音频a1作为监督信号。同时引入另一段同一目标说话人的参考特征A2。
  - 训练Transformer模型将合成源特征转换为目标说话人特征，输出预测特征ê1和波形ê1。推理时直接使用真实源音频，无需合成。
  - 关键创新：通过“合成→真实”的训练范式，使模型学会从合成数据推广到真实输入；并且引入波形级的说话人验证损失（来自ECAPA-TDNN），直接优化说话人相似性。

- **关键技术细节**：
  - **三阶段训练**：
    - Stage 1：预训练HiFi-GAN声码器，将WavLM特征重建成波形，使用MR-STFT损失和对抗训练（MPD、MSD）。
    - Stage 2：训练Transformer（6层，16头，隐层1024维，77M参数），使用L1损失（预测特征与真实目标特征）和说话人损失（基于参考波形a2与合成波形ê1的余弦相似度和L1距离）。
    - Stage 3：对Transformer输出的特征进行声码器后训练，使其适应转换后的特征分布，提高自然度。
  - **KNN-VC**：离线生成合成源特征，需要源和目标特征之间的KNN匹配，此处使用WavLM第6层特征。
  - **说话人损失**：使用ECAPA-TDNN提取说话人嵌入和隐藏表示，计算余弦相似度和L1距离，直接监督说话人身份一致性。
  - **数据增强**：训练时随机配对不同说话人，每个片段生成三个不同的目标说话人。

## 3. 实验设计：数据集、场景、基准、对比方法

- **数据集**：
  - 训练：LibriSpeech（960小时英语）；测试：LibriSpeech（英语）及Multilingual LibriSpeech（荷兰语、法语、德语、意大利语、波兰语、葡萄牙语、西班牙语等）。
- **场景**：
  - 零样本任意对任意语音转换，源和目标说话人均未在训练中出现。
  - 测试时仅提供短参考片段（3/10/30/60秒）作为目标说话人参考。
- **基准与对比方法**：
  - 对比了四个近期系统：Seed-VC（diffusion transformer）、KNN-VC（基线）、Vevo（自监督解耦）、OOVC（合成数据驱动）。其中KNN-VC是本文方法的核心基线。
  - 评估指标：说话人相似度（Spk Sim）、等错误率（EER）、词错误率（WER）、字符错误率（CER）；主观评价：平均意见得分（MOS）、说话人相似度MOS（SMOS）；多语言使用DNS-MOS。
  - 说话人验证用Redimnet（不同于训练使用的ECAPA-TDNN，确保公平），ASR用Whisper-Large-V3。

## 4. 资源与算力

- 论文中明确提到的算力信息有限：
  - 模型参数量：Transformer 77M，声码器16M（两次训练）。
  - 训练步数：Stage 1声码器100K步，Stage 2 Transformer 800K步，Stage 3声码器100K步。
  - 优化器：Adam，学习率3×10⁻⁴。
  - **未明确说明**使用的GPU型号、数量和总训练时长。仅提到训练基于LibriSpeech 960小时数据。

## 5. 实验数量与充分性

- **实验数量**：
  - 在英语上进行全面的客观（Spk Sim, EER, WER, CER）和主观（MOS, SMOS）评估，覆盖4种提示时长（3/10/30/60秒），每个时长三种随机种子，共12组测试条件。
  - 非英语多语言评估：7种语言，同四种时长，评估Spk Sim、WER和DNS-MOS。
  - 消融实验：对比有无声码器后训练（Vocoder post-training），使用DNS-MOS、Spk Sim、WER三个指标。
- **充分性与公平性**：
  - 对比方法均选取了各自的最优设置或官方实现，且评估时使用了与训练不同的说话人验证模型，保证公平。
  - 主观评价由至少10名参与者进行，结果统计合理。
  - 综合来看，实验较为充分，覆盖了多种语言、不同参考长度、消融，但缺少对计算效率或实时性的评测。

## 6. 论文的主要结论与发现

- 提出的回文训练框架显著提升了说话人相似度和EER，在所有提示时长上均优于KNN-VC、Seed-VC、Vevo、OOVC等基线，且WER/CER和主观评价保持相当水平。
- 仅在英语数据上训练，却在多种非英语语言上实现了出色的WER（优于其他方法），说明具有较强的跨语言泛化能力。
- 声码器后训练阶段有效减少了伪影，提高了DNS-MOS（自然度），且不损害说话人相似度和WER。
- 说话人验证损失的性能增益显著，是保持身份一致性的关键。

## 7. 优点

- **方法创新**：提出“回文”训练范式，利用KNN-VC合成源特征，将非平行数据转化为监督信号，避免了解耦缺陷。
- **说话人损失**：在波形级直接使用预训练说话人验证模型进行监督，显著提升身份保留。
- **数据高效**：仅用960小时英语数据训练，即可在多种语言上取得优异表现，无需多语言微调。
- **鲁棒性**：在短参考（3秒）下表现突出，克服了KNN-VC在单样本场景下的退化问题。
- **客观公正**：评估使用不同于训练阶段的说话人验证模型，并与多篇近期工作在同一条件下对比。

## 8. 不足与局限

- **算力与效率未明确**：未报告GPU型号、训练耗时、推理速度，无法评估实际资源需求或是否适合实时场景。
- **实验覆盖有限**：缺乏对非常见语言（如中文、日语）的评估；缺少对噪声环境、不同麦克风条件、情感风格等更复杂场景的测试。
- **依赖WavLM和ECAPA-TDNN**：方法的性能高度依赖这些预训练模型的质量，如模型更新可能需要重新训练。
- **潜在偏差风险**：训练数据仅英语（LibriSpeech朗读语音），可能在其他口音、风格或更自由语音上泛化能力有限。
- **未与最新大规模模型（如GPT-SoVITS、VoiceCraft）对比**：论文发表于2026年，应对比同时期SOTA，但文中只列举了2025年及之前的基线（Seed-VC 2024，Vevo 2025等）。
- **消融实验仅一项**：未充分分析说话人损失、KNN参数、Transformer层数等超参数的影响，结论的稳健性可进一步提升。

（完）
