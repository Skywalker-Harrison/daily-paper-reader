---
title: "LLM can Read Spectrogram: Encoder-free Speech-Language Modeling"
title_zh: 大语言模型可以读取频谱图：无编码器的语音-语言建模
authors: "Ruchao Fan, Yiming Wang, Yuxuan Hu, Bo Ren, Yufei Xia, Xiaofei Wang, Yao Qian, Jinyu Li"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.10231v1"
tags: ["query:speech-tech"]
score: 8.0
evidence: 无编码器语音语言模型用于语音识别和合成
tldr: 传统Speech-LLM依赖预训练语音编码器提取语义特征。本文提出Mel-LLM，直接将轻度预处理的Mel频谱图patches通过线性投影送入LLM，免去专用编码器，让LLM自主学习语音-文本对齐。在ASR任务中，该无编码器方案性能与基于编码器的方案相当，仅在数据有限时需要多模态检查点初始化。工作验证了统一无编码器架构在自回归语音-文本建模中的可行性，简化了模型设计。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10231v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 822, \"height\": 615, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10231v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 805, \"height\": 641, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10231v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 895, \"height\": 246, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10231v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1843, \"height\": 294, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10231v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1839, \"height\": 291, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10231v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1837, \"height\": 397, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10231v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 898, \"height\": 328, \"label\": \"Table\"}]"
motivation: 探索LLM能否直接学习Mel频谱图而不依赖专用语音编码器，以简化模型结构。
method: 提出Mel-LLM，将Mel频谱图分块后线性投影到LLM嵌入空间，通过LLM自身参数学习语音-文本对齐。
result: ASR任务达到与编码器方案竞争的性能，数据有限时多模态初始化关键；TTS初步可行但未达最优。
conclusion: 无编码器架构可有效用于自回归语音-文本建模，为统一模型提供新思路。
---

## 摘要
近期基于语音的大语言模型（Speech-LLMs）依赖于预训练的语音编码器将音频转换为LLM可消费的语义丰富表征。然而在本工作中，我们探索：大语言模型能否直接学习读取梅尔频谱图，而无需专门的语音编码器？我们提出Mel-LLM，一种无编码器的Speech-LLM，通过线性投影将经过简单预处理的梅尔频谱图块直接输入LLM，使LLM能纯粹通过自身参数学习语音-文本对齐。我们在自动语音识别（ASR）和文本转语音（TTS）任务上进行了大量实验。对于ASR，我们在OpenASR排行榜公开集和生产级扩展实验上进行评估，表明无编码器方案仅比使用编码器初始化的方案有轻微性能下降，具有竞争力。我们发现当数据有限时，从多模态检查点（Phi-4-MM）进行初始化对于保持性能至关重要。我们还进行了消融研究，揭示了哪些LLM层与语音编码相关性较低。对于TTS，我们展示了使用下一词元VAE方法的初步结果。虽然TTS性能尚未达到最优，但这些结果确立了完全统一的无编码器架构用于自回归语音-文本建模的可行性。

## Abstract
Recent speech-aware large language models (Speech-LLMs) rely on a pre-trained speech encoder to convert audio into semantic-rich representations consumable by LLM. In this work, instead, we explore: can an LLM learn to read Mel spectrogram directly without a dedicated speech encoder? We propose Mel-LLM, an encoder-free Speech-LLM that feeds lightly pre-processed Mel spectrogram patches directly into the LLM through a linear projection, allowing the LLM to learn speech-text alignment purely through its own parameters. We conduct extensive experiments on both automatic speech recognition (ASR) and text-to-speech (TTS) tasks. For ASR, we evaluate on the OpenASR leaderboard public sets and production-level scaling experiments, demonstrating that the encoder-free solution achieves competitive performance with only limited degradation compared to encoder-initialized counterparts. We find that when data is limited, initialization from a multimodal checkpoint (Phi-4-MM) is crucial for maintaining performance. We also present ablation studies revealing which LLM layers are less relevant to speech encoding. For TTS, we show preliminary results with a next-token VAE approach. While TTS performance is not yet optimal, these results establish the feasibility of a fully unified encoder-free architecture for autoregressive speech-text modeling.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：能否让大语言模型直接读取梅尔频谱图（Mel spectrogram），而不依赖专用的预训练语音编码器？  
- **背景**：传统 Speech-LLM 采用“语音编码器-模态投影器-LLM”的三段式架构，编码器通常很大（如 Whisper 的 600M+ 参数），存在计算开销高、表征与 LLM 内部处理不匹配、信息瓶颈等局限。受视觉领域无编码器模型（如 Fuyu、Tuna-2）启发，作者探索 LLM 自身是否有能力学习频谱特征。  
- **整体含义**：若成功，可以简化模型架构，降低参数与计算成本，为统一语音-文本理解与生成提供更简洁的方案。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：Mel-LLM —— 去除传统语音编码器（Transformer/Conformer 块），仅保留用于降采样的轻量卷积层，将梅尔频谱图分块后通过线性投影直接送入 LLM，让 LLM 的 Transformer 层自己学习“语音编码”功能。  
- **关键技术细节**：
  - **输入处理**：提取 80 维 log-Mel 频谱图 → 均值方差归一化（MVN）→ 可选降采样卷积层（时间缩减因子 r，ASR 用 r=8，TTS 用 r=2）→ 单层线性投影映射到 LLM 隐藏维度。
  - **LLM 处理**：基于 Phi-4-MM（多模态检查点），使用 LoRA 微调（rank=320, α=640），冻结基座 LLM 层。
  - **ASR 路径**：投影后的语音嵌入与文本提示拼接，LLM 自回归生成译文。
  - **TTS 路径**：基于 MELLE 框架，LLM 自回归生成隐藏状态 → 经 VAE 解码器（μ, σ → 重参数化 → 残差 MLP → PostNet）生成连续梅尔帧 → HiFi-GAN 声码器合成波形。训练损失包括重构损失（L1+MSE）、KL 散度、停止预测损失、频谱流损失。
- **公式（文字说明）**：线性投影：Es = W_proj·e + b_proj；VAE 解码：z = μ + σ·ε，ˆm = z + MLP_res(z)，最终经 PostNet 细化。

## 3. 实验设计：数据集 / 场景 / Benchmark / 对比方法

- **ASR 实验**：
  - 训练数据：公开英语语料库共约 64k 小时（LibriSpeech 960h、GigaSpeech 10k h、MLS English 44k h、SPGISpeech 5k h、CommonVoice 15、VoxPopuli、TED-LIUM、AMI、Earnings-22、FLEURS 等）。
  - 评估：OpenASR 排行榜的 8 个测试集（AMI、Earnings22、GigaSpeech、LS-clean、LS-other、SPGISpeech、TED-LIUM、VoxPopuli），指标为 WER。
  - 对比方法：Whisper-Large-V3、Phi-4-MM（无微调）、Phi-4-MM + 微调（使用预训练编码器）、随机初始化编码器、Mel-LLM（Phi-4-MM 初始化）、Mel-LLM（随机初始化）。
  - 生产规模实验：额外使用 10× 匿名内部数据（Call Center、Conversation、Dictation）。
- **TTS 实验**：
  - 训练数据：Libriheavy 50k 小时。
  - 评估：LibriSpeech test-clean 零样本文本转语音，指标为 WER（Whisper-large-v3 测量可懂度）和 UTMOS（感知质量）。
  - 对比：不同初始化、不同归一化、不同 VAE 变体（fix-KL、sigma-VAE）以及 dropout 效果。

## 4. 资源与算力

- 文中明确说明：使用 **16 块 NVIDIA H100 GPU**，配合 DeepSpeed ZeRO Stage-1 优化。训练配置：AdamW 优化器，峰值学习率 1e-4，线性 warmup-decay（9000 warmup steps），梯度裁剪 1.0，有效 batch size 512。公开数据训练 3 个 epoch，内部 10× 数据训练 1 个 epoch。TTS 使用 Libriheavy 训练 5 个 epoch。未提供总训练时长。

## 5. 实验数量与充分性

- **ASR 方面**：
  - 主实验（Table 1）：5 个系统对比，8 个测试集，共 40+ 个 WER 指标。
  - 数据扩展实验（Table 2）：3 个场景 + 平均，两种数据规模共 8 个对比。
  - 消融实验：时间缩减率（Table 3：6 个 token rate 对比），层冻结（Table 4：5 种冻结策略）。
- **TTS 方面**：
  - 仅 1 个表格（Table 5）：5 种配置对比（随机初始化、无归一化、MVN、dropout 0.1、fix-KL、sigma-VAE）。
- **充分性评价**：ASR 实验较充分，覆盖多数据集、多对比、多消融，且包含生产规模验证，公平性较好（均在同一训练框架下比较）。TTS 实验仅初步结果，对比种类较少，未与 SOTA TTS 系统直接比较，属于可行性验证。

## 6. 论文的主要结论与发现

- **ASR 无编码器方案可行**：Mel-LLM 在 OpenASR 上平均 WER 7.12%，仅比随机初始化编码器方案高 0.15%，比完全随机初始化 LLM 低 0.32%。与同时发布的 Gemma 4 无编码器方案方向一致。
- **多模态初始化在数据有限时关键**：使用 Phi-4-MM 初始化 LoRA 至关重要；随机初始化在公开数据上性能下降。
- **数据扩展缩小差距**：使用 10× 内部数据后，无编码器模型与有编码器模型的 WER 相对差距从 11.3% 缩小到 3.8%。
- **降采样率与性能-速度权衡**：12.5Hz token rate 实现 1.57× 训练加速，且性能接近 50/100Hz；6.25Hz 质量下降明显。
- **LLM 底层贡献语音编码**：冻结 LLM 0–23 层性能下降显著，而冻结 24–31 层几乎无影响，说明上层已具备高级语义能力，底层需学习声学处理。
- **TTS 初步可行**：使用 VAE 解码器可生成可听语音，但质量远未达最优；随机初始化失效，Phi-4-MM 初始化、足够 dropout、MVN 归一化有助于改善。

## 7. 优点

- **架构创新**：首次在语音领域系统验证无编码器 Speech-LLM 的可行性，简化了传统三段式架构。
- **实验严谨**：ASR 部分包含公开 Leaderboard + 生产规模扩展 + 多种消融，对比条件控制良好；揭示了 LLM 层功能分工。
- **效率优势**：消除编码器后获得训练加速（12.5Hz 下 1.57×），减少参数与计算开销。
- **初步统一框架**：ASR 与 TTS 共享同一 LLM 骨干，未来可联合训练。

## 8. 不足与局限

- **TTS 性能不理想**：WER 高达 11–18%，UTMOS 仅 3.1–3.3，远未达到实用水平，未与 SOTA TTS 模型（如 MELLE、Vall-E）直接对比。
- **未实现联合 ASR+TTS 训练**：本文分别训练两个任务，未探索一端到端的统一模型。
- **数据依赖**：公开数据下无编码器方案性能仍弱于有编码器方案（差距小于 1% 绝对 WER，但相对差距多个点）；需大规模数据才能逼近。
- **计算资源描述不全**：仅提供 GPU 数量和 batch size，未给出训练时间或 GPU 时长，无法复现精确开销。
- **评估偏差风险**：TTS 仅用 Whisper ASR 和 UTMOS 评价，缺乏主观听感测试。生产级 ASR 实验内部数据匿名化，但未公开具体统计信息。
- **应用限制**：仅针对英文展开；降采样率与 ASR/TTS 的联合最优设置未探索；LoRA 参数量大（Rank 320）可能仍会引入额外负担。

（完）
