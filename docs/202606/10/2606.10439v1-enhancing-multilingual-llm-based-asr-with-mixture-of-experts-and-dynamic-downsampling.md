---
title: Enhancing Multilingual LLM-based ASR with Mixture of Experts and Dynamic Downsampling
title_zh: 利用混合专家和动态降采样增强多语言基于LLM的ASR
authors: "Guodong Lin, Ziqi Chen, Yuxiang Fu, Ke Li, Wei-Qiang Zhang"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.10439v1"
tags: ["query:speech-tech"]
score: 9.0
evidence: 多语言自动语音识别，使用混合专家和动态下采样
tldr: 针对多语言泛化和模态对齐挑战，提出基于投影器的LLM-ASR框架。通过混合专家架构增强跨语言适应性，利用连续整合与激发机制实现动态降采样和模态对齐。实验表明该方法显著超越强基线，为构建准确鲁棒的LLM-ASR系统提供了有效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10439v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 906, \"height\": 723, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10439v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 862, \"height\": 389, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10439v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 887, \"height\": 580, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10439v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1850, \"height\": 393, \"label\": \"Table\"}]"
motivation: 解决多语言ASR中LLM集成时的跨语言泛化能力不足和模态对齐困难。
method: 采用混合专家架构和连续整合与激发机制进行动态降采样和模态对齐。
result: 在多个语言上超越强基线模型，性能显著提升。
conclusion: 所提方法可构建更准确、鲁棒和泛化的LLM-ASR系统。
---

## 摘要
大语言模型（LLM）的快速发展为自动语音识别（ASR）开辟了新的前沿，使得它们的高效集成成为关键且具有挑战性的研究方向。为此，本文提出了一种基于投影器的LLM-ASR框架，针对多语言泛化和模态对齐的关键挑战。我们的方法采用了混合专家（MoE）架构以提高跨语言适应性，以及连续积分-触发（CIF）机制用于动态降采样和模态对齐。实验结果表明，这些组件的组合带来了显著的性能提升，超越了强基线模型。所提出的方法朝着构建更准确、更鲁棒且更具泛化性的基于LLM的ASR系统迈出了一步。

## Abstract
The rapid progress of large language models (LLMs) has opened up a new frontier for automatic speech recognition (ASR), making their effective integration a critical and challenging research direction. To this end, this work proposes a projector-based LLM-ASR framework targeting the key challenges of multilingual generalization and modality alignment. Our approach incorporates a Mixture of Experts (MoE) architecture to improve cross-lingual adaptability, and a Continuous Integrate-and-Fire (CIF) mechanism for dynamic downsampling and modality alignment. Experimental results show that the combination of these components yields substantial performance improvements, surpassing strong baseline models. The proposed method represents a step toward building more accurate, robust, and generalizable LLM-based ASR systems.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义

- **研究动机**：大语言模型（LLM）的语义理解能力为自动语音识别（ASR）带来新机遇，但将LLM有效集成到ASR面临两大挑战：
  - **多语言泛化能力不足**：不同语言的音素、语法差异大，简单的线性投影器难以适应。
  - **模态对齐困难**：音频特征帧率（50-100 Hz）与文本token序列长度严重不匹配，固定降采样导致信息损失或对齐错误。
- **整体含义**：本文旨在通过改进投影器模块，增强LLM-ASR框架在多语言场景下的准确性和鲁棒性，推动端到端语音识别系统的实用化。

### 方法论：核心思想与关键技术

- **整体框架**：Encoder–Projector–LLM 架构。采用 Whisper-large-v3 作为语音编码器，Qwen-2.5 7B 作为冻结的LLM解码器，仅训练投影器模块。
- **MoE-Enhanced Projector（混合专家投影器）**：
  - 保留卷积层作为共享骨干，增加专家子网络，专家数量等于训练语言数。
  - 门控网络根据声学特征计算每个专家的激活权重 \( g_k(x) \)，最终输出为加权组合：  
    \[ y = \sum_{k=1}^{K} g_k(x) E_k(x) \]
  - 动态选择专家可灵活适应不同语言的声学-文本映射，提升跨语言泛化。
- **CIF-Based Modality Alignment（基于连续积分-触发的动态降采样）**：
  - CIF 模块为每帧分配权重（0~1），累积超过阈值（如1）时“触发”输出一个token，实现可变长度降采样。
  - 为解决标准CIF过度压缩导致信息丢失的问题，采用**修改版CIF**：训练目标设为输出长度是目标token序列长度的n倍（n=4），使有效降采样率与基线（固定4倍）相当，同时保留对语速变化的适应能力。
  - CIF预测器先单独预训练（冻结编码器），之后插入投影器并冻结，再联合训练整个投影器。

### 实验设计

- **数据集**：
  - **训练集**：MLC-SLM Challenge提供的1500小时多语言数据（11种语言：英、法、德、意、日、韩、葡、俄、西、泰、越，英语占500小时，其余各100小时）。
  - **域内评估**：MLCSLM-dev（开发集）。
  - **域外评估**：FLEURS-test 和 CommonVoice-test（相同11种语言）。
  - 扩展实验：额外加入6500小时来自CommonVoice、GigaSpeech2、LibriSpeech、MLS、VoxPopuli的随机采样，构成8000小时训练集。
- **基准模型**：
  - **Whisper-large-v3**（直接使用，无投影器）。
  - **LLM-ASR Baseline**：两层卷积+两层线性投影器，固定4倍降采样。
- **对比方法**：
  - Baseline + MoE Projector
  - Baseline + 标准CIF降采样器
  - **Proposed**（MoE + modified CIF），分别在1500小时和8000小时上训练。
- **评估指标**：词错误率（WER）。

### 资源与算力

- **硬件**：6块 NVIDIA A40 GPU（每块48 GB显存）。
- **训练配置**：
  - 优化器：Adam，最大学习率1e-3，前2500步线性预热后逆平方根衰减。
  - 训练轮次：最多20个epoch，早停基于验证损失。
  - 动态批次策略：每批最多5000帧。
- **未明确信息**：未说明具体训练时长（小时或天数）、模型参数量、推理速度等。

### 实验数量与充分性

- **实验组数**：
  - 主要对比表（Table 1）包含6行结果：Whisper、Baseline、+MoE、+标准CIF、Proposed（1500h）、Proposed（8000h）。
  - 图3给出了11种语言的逐语言WER对比（Baseline vs Proposed）。
  - 消融实验覆盖了MoE和CIF两个组件的单独影响。
- **充分性评价**：
  - **优点**：覆盖域内/域外、消融、数据规模缩放三种场景，对比基线和强模型Whisper，语言级别分析细致。
  - **不足**：未与其他LLM-ASR方法（如Salmon、LLM-ASR-Linear等）直接比较；仅测试一种MoE配置（专家数等于语言数）和一种CIF放松因子（n=4），缺乏超参数敏感性分析；未进行交叉验证或统计显著性检验。

### 主要结论与发现

1. **MoE投影器显著提升多语言ASR性能**：将MLCSLM-dev的WER从23.26%降至16.10%，在所有语言上几乎一致下降。
2. **标准CIF降采样反而导致性能下降**（WER升至18.95%），说明过度压缩引起信息损失。
3. **修改版CIF（n=4）保持适应性同时性能最佳**：结合MoE后在1500小时训练下WER降到15.27%，全面优于Whisper（21.48%）和Baseline。
4. **数据扩展主要改善域外泛化**：8000小时训练后CommonVoice和FLEURS大幅下降，但域内MLCSLM-dev略有上升（15.45%），暗示域内过拟合风险。

### 优点

- **方法创新**：将MoE应用于LLM-ASR投影器，通过语言特定专家提升跨语言适配性；引入CIF实现动态降采样，并针对信息损失提出改进策略。
- **实验设计严谨**：对比多个基线（包括强模型Whisper），分别消融各组件，覆盖域内、域外及数据规模效应，提供逐语言分析。
- **结果显著**：在三个测试集上均取得最佳WER，验证了方法的有效性与泛化性。

### 不足与局限

- **实验覆盖**：
  - 仅使用一套模型组合（Whisper encoder + Qwen LLM），未验证其他编码器或LLM下的泛化性。
  - 多语言数据集仅涵盖11种语言（多数为欧洲及东亚大资源语言），低资源语言或方言表现未知。
  - 未与端到端训练（如LLM解冻）或更复杂的对齐方法（如Q-Former）对比。
- **方法局限**：
  - CIF放松因子n=4为手动选取，缺乏自动搜索或理论指导。
  - MoE的专家数量与语言数量捆绑，当扩展新语言时需增加专家，可能带来扩展性问题。
  - 未讨论MoE和CIF带来的额外计算开销（参数量、推理延迟）。
- **偏差风险**：训练集的英语占比远高于其他语言（500h vs 100h），可能引入语言不平衡偏差；结果中未报告置信区间或标准差。
- **应用限制**：仅适用于具有投影器的LLM-ASR架构，对于直接使用LLM作为解码器的统一模型（如SpeechLLaMA）不直接适用。

（完）
