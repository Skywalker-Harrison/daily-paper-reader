---
title: "Thaka at KSAA-2026 Task 2: Regularized Fine-Tuning for Arabic Speech Diacritization"
title_zh: Thaka在KSAA-2026任务2：阿拉伯语语音变音符的正则化微调
authors: "Meshal Alamr, Hassan Alqaeri, Abdullah Aldahlawi"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25928v1"
tags: ["query:speech-tech"]
score: 8.0
evidence: 微调Whisper实现阿拉伯语音频的自动变音符号标注，涉及阿拉伯语语音识别
tldr: "阿拉伯语音译转写任务面临训练数据稀缺（仅2,327样本）且禁止使用外部数据的严格约束。本工作提出正则化微调策略，基于字符级多模态模型CATT-Whisper（融合Whisper语音编码器与CATT文本编码器），采用R-Drop一致性正则化、Optuna自动优化高权重衰减超参数和Focal Loss来抑制过拟合。推理时对四个检查点进行200次蒙特卡洛Dropout平均以提升稳定性。在KSAA-2026任务2上取得23.26%WER，夺得冠军。该方法证明强正则化可有效应对低资源语音后处理任务，具有重要实践意义。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25928v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1626, \"height\": 890}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.25928v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 785, \"height\": 508}, {\"url\": \"assets/tables/arxiv/2605.25928v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 777, \"height\": 527}, {\"url\": \"assets/tables/arxiv/2605.25928v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 788, \"height\": 270}, {\"url\": \"assets/tables/arxiv/2605.25928v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 716, \"height\": 313}]"
motivation: 针对训练数据极少且禁止外部数据的阿拉伯语音译转写任务，需设计强正则化方法防止过拟合。
method: 正则化微调CATT-Whisper，结合R-Drop一致性正则化、Optuna优化高权重衰减和Focal Loss，推理时对四个检查点进行200次蒙特卡洛Dropout概率平均。
result: "在KSAA-2026任务2官方指标上实现23.26%词错误率，在参赛队伍中排名第一。"
conclusion: 本研究证明强正则化可有效应对低资源语音后处理任务，无需外部数据即可获得最优性能。
---

## 摘要
我们描述了KSAA-2026阿拉伯语语音听写自动变音符共享任务任务2的获胜系统。该任务要求从语音音频和无变音符的转录文本中生成完全带变音符的阿拉伯语文本，仅有2,327个训练样本可用，且不允许使用外部数据。我们的系统微调了CATT-Whisper，这是一个字符级多模态模型，结合了预训练的CATT文本编码器和冻结的Whisper语音编码器。我们方法的关键是训练正则化：R-Drop一致性正则化、Optuna优化的高权重衰减超参数以及Focal Loss。在推理时，我们使用蒙特卡洛Dropout在softmax概率级别上对四个模型检查点进行200次随机前向传播的平均。该系统在主要排行榜指标（包括词尾，包括无变音符位置）上实现了23.26%的词错误率，在所有参与者中排名第一。

## Abstract
We describe the winning system for Task 2 of the KSAA-2026 Shared Task on Arabic Speech Dictation with Automatic Diacritization. The task requires producing fully diacritized Arabic text from speech audio and undiacritized transcripts, with only 2,327 training samples available and no external data permitted. Our system fine-tunes CATT-Whisper, a character-level multimodal model combining a pretrained CATT text encoder with a frozen Whisper speech encoder. The key to our approach is training regularization: R-Drop consistency regularization, Optuna-optimized hyperparameters with high weight decay, and Focal Loss. At inference, we average 200 stochastic forward passes across four model checkpoints using Monte Carlo Dropout at the softmax probability level. The system achieves 23.26% WER on the primary leaderboard metric (with case endings, including no-diacritic positions), placing 1st among all participants.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **任务背景**：阿拉伯语文本通常不带变音符（短元音和语音标记），恢复这些符号对于文本转语音、机器翻译和语言学习至关重要，但纯文本模型存在歧义，尤其在方言阿拉伯语中。
- **问题定义**：KSAA-2026 共享任务的任务2要求从语音音频和未带变音符的转录文本中生成完全带变音符的阿拉伯语文本。该任务面临**极低资源**的严峻挑战：仅有 **2,327 个训练样本**，且**禁止使用任何外部数据**。
- **研究动机**：在如此严格的数据限制下，如何通过强正则化方法充分利用有限数据、防止过拟合，是提升系统性能的关键。论文旨在探索正则化微调策略，并验证其在多模态语音变音符任务中的有效性。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 基于已有的 **CATT-Whisper** 多模态架构（冻结的 Whisper 语音编码器 + CATT 文本编码器），采用**强正则化训练配方**与**推理时蒙特卡洛 Dropout 集成**，最大化利用有限的训练数据。

### 关键技术细节
- **模型架构**：
  - 文本编码器：6层 CATT Transformer（d=512, 16头），预训练于阿拉伯语变音符任务，输出15个变音符类别。
  - 语音编码器：冻结的 Whisper-base（6个编码器块，d=512）。
  - 融合方式：前缀添加（prefix addition）。将1500个Whisper帧平均池化为150个标记，经过线性投影后加到150个专用前缀位置，位于文本输入之前。
  - 总参数量约39M，可训练约19M。
- **训练正则化**：
  - **R-Drop 一致性正则化**：每个输入以不同dropout掩码经过两次前向传播，对两个预测分布施加对称KL散度惩罚（α=2.08），鼓励一致性。
  - **Focal Loss**：γ=0.34，缓解类别不平衡。
  - **标签平滑**：ε=0.018。
  - **高权重衰减**：weight decay=0.098。
  - **语音嵌入dropout**：训练时随机置零语音表示（p=0.09），增强模态鲁棒性。
  - **数据增强**：SpecAugment（频域掩码10，时域掩码63）和高斯噪声注入（SNR 10–30 dB）。
- **超参数优化**：使用 **Optuna** 进行30次试验，每次12个epoch，自动搜索最优组合（见表1）。
- **训练多检查点**：训练了4个检查点（3个使用相同配置不同随机种子42/7/123；第4个使用替代配置以增加多样性：lr=4.7e-5, batch=32, γ=1.0, 标签平滑0.108，并在第15个epoch后解冻4个Whisper块）。
- **推理时集成**：
  - 所有检查点均保持CATT编码器dropout激活（p=0.1），LayerNorm保持评估模式。
  - 每个模型进行50次随机前向传播（共4×50=200次），对softmax概率取平均，再取argmax得到最终预测。
- **后处理**：基于CATT的字符级1:1对应关系，强制三个不变性：去除变音符后恢复原始输入；变音符数量与预测匹配；消耗所有字母位置。

## 3. 实验设计

### 数据集
- **KSAA-2026 Shared Task官方数据集**：
  - 训练集：2,327个样本，经筛选（变音符字符比例<0.6的过滤后）得到**2,187个有效训练样本**。
  - 开发集：260个样本。
  - 测试集：328个样本。
  - 样本为短语音（平均约7秒），涵盖多种阿拉伯方言。
- **禁止使用任何外部数据**。

### 评估指标
- **主要指标**：WER（词错误率，包含词尾变音符及无变音符位置），DER（变音符错误率），SER（句子错误率）。排名以WER为准。

### 对比方法
- **共享任务Baselines**（见表2）：
  - 纯文本基线（text-only）
  - 文本+ASR基线（basic & fine-tuned）
  - 其他参赛者系统
- **内部消融实验**（见表3）：
  - CATT-Whisper原始预训练模型
  - CATT-Whisper简单微调（lr=1e-5, 交叉熵, 30 epoch）
  - +正则化配方（R-Drop, Focal Loss, 高权重衰减, Optuna调参）
  - +4个模型MC Dropout集成

### 实验结果
- 测试集WER：**23.26%**，在所有参赛者中排名第一（第二名WER 24.39%）。
- 消融实验表明：正则化配方带来3.25个百分点的WER下降（从30.43%到27.18%），MC Dropout集成额外贡献1.16个百分点（从27.18%到26.02%）。
- 文字案例（表4）展示了系统正确标注复杂句子的效果。

## 4. 资源与算力

- 论文**未明确说明**使用的GPU型号、数量及总训练时长。
- 已知：微调CATT-Whisper模型（约19M可训练参数），采用Optuna进行30次超参数搜索（每次12 epoch），最终训练4个检查点。推理时每个测试样本需执行200次前向传播，计算开销较大。
- 从时间节点（2026年）推测可能使用现代消费级或云端GPU，但无法确认具体配置。

## 5. 实验数量与充分性

### 实验数量
- **内部消融实验**：在开发集上进行了一组累积消融（从预训练→简单微调→正则化配方→MC Dropout集成），共4个配置。
- **超参数搜索**：Optuna进行了30次试验，但未列出所有探索组合。
- **架构探索**：尝试了交叉注意力融合、CRF解码、注意力池化、辅助头、强化学习微调等，均未改善，但未给出详细实验结果和数据。
- **与其他参赛者对比**：在测试集上对比了6个参赛队伍和3个官方基线。
- **仅在一个开发集和一个测试集上评估**，未做交叉验证或多折划分。

### 充分性与公平性
- **优点**：消融实验逐步增加组件，清晰地展示了正则化配方和集成方法的贡献。与参赛者公平统一在共享指标上比较。使用标准WER/DER/SER指标。
- **不足**：
  - 缺乏对每个正则化组件（R-Drop、Focal Loss、高权重衰减、标签平滑、语音dropout等）的独立消融，无法判断哪些组件贡献最大。
  - 未报告模型在不同方言或不同语言环境下的性能分布，可能掩盖偏差。
  - 实验仅在单一小规模数据集上进行，泛化性未知。
  - 未提供统计显著性检验或置信区间。
  - 论文提到探索其他架构但未给出具体数值，难以评估这些尝试的严肃性。

## 6. 论文的主要结论与发现

- **正则化训练配方比架构修改更重要**：在仅有约2,000训练样本的条件下，通过R-Drop、Focal Loss、高权重衰减等正则化手段获得了最大的性能提升（3.25个百分点），而尝试的多种架构变更均未超越基线。
- **多检查点MC Dropout集成有效**：4个检查点各50次随机前向传播的软概率平均进一步降低了WER（额外1.16个百分点），但代价是推理速度慢约50倍。
- **语音模态至关重要**：带有语音特征的模型显著优于纯文本模型，确认了多模态信息的优势。
- **系统最终在KSAA-2026任务2中排名第一**，WER 23.26%。

## 7. 优点

- **方法简洁高效**：未使用复杂的外部资源或大规模模型，而是专注于正则化策略，避免了过度工程化。
- **自动超参数优化**：使用Optuna系统搜索，节省人工调参时间，并针对小数据集找到最佳平衡。
- **推理时MC Dropout集成**：利用训练时的dropout作为贝叶斯近似，无需额外训练多个完整模型，即可获得集成效果，提升了鲁棒性。
- **后处理强制一致性**：确保输出与输入文本完全对齐，避免替换错误。
- **有效的模态鲁棒训练**：语音embedding dropout增强了模型对语音特征缺失的容忍度。

## 8. 不足与局限

- **计算开销**：推理时200次前向传播，延迟高（约为单次50倍），不适合实时应用。论文未讨论如何权衡准确率与效率。
- **消融不完整**：每个正则化组件的独立贡献未被量化，不能确定R-Drop、Focal Loss、高权重衰减等各有多大作用。
- **单数据集、无跨域验证**：仅在KSAA-2026的特定数据集上实验，该数据集规模小、方言覆盖有限，系统在更大或不同领域的阿拉伯语数据上表现未知。
- **未分析方言间性能差异**：尽管数据包含多方言，但未报告每个方言的WER，可能存在质量不均或偏向某方言（如现代标准阿拉伯语）。
- **缺乏误差分析**：没有系统分析错误的类型（如变音符位置错误、词尾误标等），限制了深入改进方向。
- **复现信息不完整**：未公开代码或训练配置细节（如具体数据分割、种子等），Optuna搜索的空间也未完全描述。
- **对禁止外部数据规则的高度依赖**：一旦允许外部数据，结论可能变化，方法可迁移性需进一步验证。

（完）
