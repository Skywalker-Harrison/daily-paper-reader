---
title: Task-Vector Arithmetic for Emotional Expressivity Control in Language-Model-Based Text-to-Speech
title_zh: 基于语言模型的文本到语音中的情感表达控制的任务向量算术
authors: "Daniel Oliveira de Brito, Arnaldo Candido Junior"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.05367v1"
tags: ["query:qwen-tts"]
score: 9.0
evidence: 使用任务向量算术在Qwen3-TTS模型上实现情感控制
tldr: 本文研究任务向量算术在基于语言模型的文本转语音系统（LM-TTS）中对情感表达性的控制。通过消融实验发现，情感韵律主要由ECAPA-TDNN提取的x-vector承载。基于此，提出一种无训练方法：在x-vector空间计算中性到情感的质心方向，并应用于目标说话人。在英文ESD和跨语言巴西葡萄牙语emoUERJ数据集上，该方法在保持说话人身份和可懂度的前提下，显著提升了情感余弦相似度。结果证明质心算术可绕过token架构的兼容性问题，实现高效情感控制。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05367v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1720, \"height\": 602, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05367v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 754, \"height\": 462, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05367v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 730, \"height\": 473, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05367v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 728, \"height\": 470, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05367v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1182, \"height\": 447, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05367v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 702, \"height\": 574, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.05367v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1380, \"height\": 412, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05367v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1002, \"height\": 568, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05367v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1002, \"height\": 568, \"label\": \"Table\"}]"
motivation: 验证任务向量算术能否从模块化TTS迁移到LM-TTS，并定位情感韵律的关键载体。
method: 通过消融实验定位x-vector为情感载体，提出基于x-vector质心算术的无训练情感控制方法。
result: "跨语言情感余弦提升0.09-0.29，说话人身份相似度>0.88，词错误率≈0。"
conclusion: 质心算术在x-vector空间可有效控制LM-TTS情感，规避token架构的兼容性问题。
---

## 摘要
我们研究了在基于语言模型骨干和上下文学习的大规模文本到语音（LM-TTS）系统中，任务向量算术是否能够迁移用于跨说话人情感强度控制。通过对Qwen3-TTS-12Hz-1.7B模型上的四个逐步缩窄的操作数——通过LoRA微调的模型权重、连续编解码器嵌入、离散编解码器标记以及与合成骨干联合训练的ECAPA-TDNN编码器生成的说话人嵌入（x-vector）——进行系统性消融研究，我们将情感韵律的主要载体定位为x-vector。基于这一发现，我们提出了一种无训练方法，该方法基于x-vector空间中的质心算术：情感方向τ = 𝔼_i[x(s_i, emo)] - 𝔼_i[x(s_i, neutral)]，应用于未见过的目标说话人为x_new = x(target, neutral) + α·τ。使用ESD（英语）作为τ源，emoUERJ（巴西葡萄牙语）作为跨语言真实目标，我们在英语保留说话人上观察到emotion2vec余弦相对于ICL基线平均提升+0.29，在巴西葡萄牙语保留说话人上提升+0.09，同时基本保留身份（多说话人τ变体的WavLM SECS ≥ 0.88）和可懂度（PT-BR中WER ≈ 0）。这些结果初步表明，当算术操作在说话人嵌入上执行时，可以规避之前报道的质心算术风格控制与基于标记的TTS架构不兼容的问题。

## Abstract
We investigate whether task-vector arithmetic, successful for cross-speaker emotional intensity control in modular text-to-speech (TTS), transfers to large-scale TTS systems built on language-model backbones with in-context learning (LM-TTS). Through a systematic elimination study over four progressively narrower operands on Qwen3-TTS-12Hz-1.7B - model weights via LoRA fine-tuning, continuous codec embeddings, discrete codec tokens, and the speaker embedding (x-vector) produced by an ECAPA-TDNN encoder jointly trained with the synthesis backbone - we localize the dominant carrier of emotional prosody to the x-vector. Building on this finding, we propose a training-free method based on centroid arithmetic in x-vector space: an emotion direction $τ= \mathbb{E}_i[x(s_i,\text{emo})] -\mathbb{E}_i[x(s_i,\text{neutral})]$ applied to an unseen target speaker as $x_{\text{new}} = x(\text{target},\text{neutral}) + α\cdotτ$. Using ESD (English) as the $τ$ source and emoUERJ (Brazilian Portuguese) as a cross-lingual ground-truth target, we observe average gains of $+0.29$ in emotion2vec cosine over the ICL baseline on English held-out speakers and $+0.09$ on Brazilian Portuguese held-out speakers, while largely preserving identity (WavLM SECS $\gtrsim 0.88$ for the multi-speaker $τ$ variant) and intelligibility (WER $\approx 0$ in PT-BR). These results offer initial evidence that the reported incompatibility of centroid-arithmetic style control with token-based TTS architectures may be circumvented when the arithmetic operates on the speaker embedding.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：任务向量算术（task-vector arithmetic）在模块化TTS架构（如CFS2、VITS）中已被成功用于情感控制，但现代LM-TTS（基于语言模型骨干的文本到语音系统，如Qwen3-TTS）的架构完全不同：情感韵律通过自回归上下文学习隐式产生，而非由参数化子模块显式控制。因此，本文的核心问题是：**任务向量算术能否迁移到LM-TTS？如果可以，情感信息在LM-TTS的哪个组件中？是否可以实现无训练的跨说话人情感控制？**

- **整体含义**：本文通过系统性消融实验定位情感的主要载体为**x-vector（说话人嵌入）**，并提出一种基于x-vector空间质心算术的**无训练**情感控制方法，为LM-TTS中的情感表达提供了一种低计算成本的替代方案，同时揭示了权值空间算术在LM-TTS中失效的本质原因。

### 2. 论文提出的方法论

- **核心思想**：首先通过消融实验定位情感载体，然后利用x-vector质心算术实现情感转移，无需额外训练。

- **消融实验设计（逐步排除四个候选操作数）**：
  - **Step 1（权重空间）**：对Qwen3-TTS进行全量微调或LoRA微调，计算情感任务向量（τ = θ_ft_angry - θ_ft_neutral），发现无法产生受控的情感语音（要么噪声，要么与中性无异）。
  - **Step 2（连续编解码嵌入）**：对每个码本计算连续嵌入的质心差（τ_k = centroid_k(angry) - centroid_k(neutral)），以加和或逐层方式注入，同样导致无情感效果或退化噪声。
  - **Step 3（离散编解码标记）**：通过控制性分离实验（将愤怒话语的所有codec token与中性x-vector组合），发现LM忽略token中的情感信号，输出中性语音；说明情感主要由x-vector承载。
  - **Step 4（x-vector）**：定位为情感载体，验证x-vector算术的有效性。

- **提出的x-vector质心算术方法**：
  - 计算**源说话人集合**S上情感与中性的x-vector质心差作为情感方向：
    ```
    τ_emo = E_i[x(s_i, emo)] - E_i[x(s_i, neutral)]
    ```
  - 对**未见过的目标说话人**应用：
    ```
    x_new = x(target, neutral) + α · τ_emo
    ```
  - 其中α为推理超参数，控制情感强度。支持两种变体：**单说话人τ**（单一源说话人）和**多说话人平均τ**（avg4spk，平均4个说话人），后者可消除源说话人音色残余。

### 3. 实验设计

- **数据集与场景**：
  - **源情感方向τ提取**：使用英文语音情绪数据库**ESD**中的4个说话人（0011,0014,0017,0020）各50句愤怒/中性语音。
  - **英文保留说话人测试**（EN held-out）：ESD中未见过的2个说话人（0013,0019），n=30句/条件。
  - **跨语言巴西葡萄牙语测试**（EN→PT-BR）：使用**emoUERJ**中的3个说话人（m03,m04,w04），n≈6–14句/条件。其中m03/m04具有平行情绪-中性对，w04为跨文本设计。

- **基准（Baseline）**：纯ICL（α=0）即无操作的系统输出，以及**自然上限**（真实录音）。

- **对比方法**：单说话人τ（single0017） vs. 多说话人平均τ（avg4spk）；不同α取值扫描（0~2.5）。

- **评估指标**：
  - **EECS**（emotion2vec余弦相似度）：合成音频与真实情绪录音的情感嵌入相似度。
  - **SECS_W**（WavLM说话人嵌入余弦）：合成音频与中性参考的身份相似度（独立编码器，避免循环）。
  - **UTMOS**：自然度评分（VoiceMOS 2024冠军模型，未在PT-BR训练）。
  - **WER**：Whisper-large-v3词错误率。
  - **xvec_cos_GT**（仅PT-BR）：合成音频的ECAPA-TDNN x-vector与真实情绪录音的余弦（直接验证算术假设）。

### 4. 资源与算力

- **论文未明确说明训练所用的GPU型号、数量、时长**。仅指出与现有方法对比：IndexTTS2需要8块A100训练三周，而本方法**不需要训练**（仅需一次嵌入平均）。微调实验（Step1）中使用单说话人30分钟数据，但未报告具体算力消耗。

### 5. 实验数量与充分性

- **实验数量**：消融实验覆盖4个操作数、多种微调配置（学习率、LoRA rank、epochs等）。英文保留说话人：2个目标×3种情绪×2种τ变体×5–6个α值 = 约60组条件。跨语言：3个目标×3种情绪×2种τ变体×4–5个α值 ≈ 90组条件。每个条件有n≈6–30个样本，统计上合理。

- **充分性**：消融实验设计系统，逐步排除候选操作数，提供直接证据（如Step3的分离实验）。评估指标全面（情感、身份、自然度、可懂度）。但**通用性有限**：仅在Qwen3-TTS一个模型上验证；跨语言效果因基线偏高而被压缩；缺乏人类听感测评。

- **公平性**：基准（α=0）使用同一模型的纯ICL输出，上限使用真实录音，对比方法包括单/多说话人τ，α扫描覆盖全部范围。但未与其他显式情感控制方法（如IndexTTS2、MiniMax-Speech）进行直接比较，因为本文聚焦无训练方法，不属于同一轨道。

### 6. 论文的主要结论与发现

- **权值空间算术在LM-TTS中失效**：单说话人/单情绪微调无法产生受控情感语音，与模块化TTS不同。
- **x-vector是情感韵律的主要载体**：通过代码标记与x-vector的冲突实验证实，LM倾向于遵循x-vector的情感方向。
- **x-vector质心算术有效控制跨说话人情感**：英文保留说话人EECS提升+0.29（avg4spk），跨语言EN→PT-BR提升+0.09。
- **多说话人平均τ能更好保持身份**：avg4spk变体的SECS_W≥0.88，显著优于单说话人变体（α高时身份下降）。
- **情感强度可通过α连续调节**，无需重新合成。
- **跨语言验证成功**：英文τ可迁移至巴西葡萄牙语，且身份保持出色（xvec_cos_GT接近说话人内波动上限），证明情感方向具有语言无关性。

### 7. 优点

- **无训练**：仅需一次嵌入平均，零额外训练成本，推理时通过α控制强度。
- **设计严谨**：消融实验逐步定位情感载体，Step3的分离实验提供因果关系强证据。
- **跨语言验证**：使用英语源和葡萄牙语目标，并利用平行数据（xvec_cos_GT）直接验证算术假设。
- **公开代码和产物**：提供GitHub仓库，促进可复现性。
- **轻量高效**：与需要大量GPU时间的显式微调方法相比，本方法极其节约资源。

### 8. 不足与局限

- **通用性有限**：仅验证于Qwen3-TTS-12Hz-1.7B一个模型，对架构差异（如不同编码器设计）的鲁棒性未知。
- **编码器依赖性**：方法依赖与骨干联合训练的可学习说话人编码器；若编码器为纯验证式或具有显式解耦损失（如GRL），可能压缩情感方向窗口。
- **跨语言效果增益较小**：PT-BR基线EECS已较高（0.70–0.91），导致相对提升仅+0.09；可能源于emoUERJ中性语音的非典型性及emotion2vec对低资源语言的判别力不足。
- **缺乏人类听测**：客观指标（EECS、UTMOS）可能无法完全反映感知质量，尤其对于跨语言场景。
- **微调数据规模小**：Step1单说话人微调（~30分钟）属于低声学变异性场景，未排除多说话人多情绪大规模微调可能打开新窗口的潜在可能性（但主要发现来自Step3，不受此限）。
- **未与其他显式情感控制LM-TTS基线对比**：如IndexTTS2、MiniMax-Speech，但本文定位为无训练方法的探索性工作，侧重点不同。

（完）
