---
title: "From Monolingual to Multilingual: Evaluating Mamba for ASR in South African Languages"
title_zh: 从单语到多语：评估Mamba在南非语言ASR中的表现
authors: "Jesujoba O. Alabi, Julian Herreilers, Badr M. Abdullah, Dietrich Klakow"
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.01502v1"
tags: ["query:speech-tech"]
score: 8.0
evidence: 南非语言的多语言ASR评估
tldr: 自动语音识别（ASR）中，新提出的状态空间模型Mamba在七个南非语言上表现出色。单语实验显示，Mamba在50小时训练数据下达到与Conformer相当的正确率，但训练更快、资源消耗更少。多语训练一致优于单语，但显式加入语言嵌入未能提升域内性能，却增强了跨语料鲁棒性。研究发现语言嵌入实际充当任务特定控制向量，而非捕获语言类型学相似性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.01502v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 849, \"height\": 745, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01502v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 845, \"height\": 649, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.01502v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 785, \"height\": 395, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01502v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1534, \"height\": 334, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01502v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1443, \"height\": 787, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01502v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 657, \"height\": 411, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01502v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1509, \"height\": 219, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01502v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1421, \"height\": 419, \"label\": \"Table\"}]"
motivation: 评估Mamba模型在非洲语言ASR中的表现，探索多语训练及语言信息注入的效果。
method: 在七种南非语言上，对比Mamba与Conformer的单语性能；进行多语池化及加入语言或语系嵌入的扩展，并尝试CTC与语言识别多任务学习。
result: Mamba在准确率上与Conformer持平，训练更快；多语训练提升性能；语言嵌入在低资源时有效，但域内无提升，且嵌入不反映语言类型学关系。
conclusion: Mamba适合非洲语言ASR，多语训练有利，但语言嵌入作用有限，需进一步理解其机制。
---

## 摘要
自动语音识别（ASR）的最新进展探索了不同的序列模型，包括基于Conformer的模型和更新的状态空间模型（如Mamba）。尽管先前的工作已在多种语言中评估了这些架构，但它们在非洲语言中的有效性仍未得到充分探索。在这项工作中，我们评估了Mamba在七种南非语言上的ASR性能。在单语实验中，每个模型在每种语言上使用50小时的语音进行训练，我们将Mamba与类似参数规模的Conformer基线进行比较。Mamba在使用更少的计算资源和更快的训练速度下达到了与Conformer相似的识别准确率。我们进一步评估了这种设置下的泛化能力，发现两个模型都难以泛化到比训练数据长得多的语音。然后，我们使用Mamba模型研究了多语ASR，基线是将所有语言合并在一起。在此基础上，我们测试了三种扩展：通过将语言和语言族嵌入作为偏置添加到下采样的声学表示中，利用语言族信息进行训练；以及使用CTC ASR目标和一个语言识别（LID）头进行多任务学习。我们发现多语训练始终优于单语训练。然而，添加显式语言信息并不能改善域内性能，但确实提高了跨语料库的鲁棒性。我们在低资源多语设置中进行了消融研究，使用了每种语言5小时和10小时的训练数据，观察到使用语言嵌入带来的收益，并进一步证明移除或改变它们会损害模型性能。最后，我们分析了这些嵌入，发现它们并未在类型学意义上捕捉语言相似性，而是作为特定任务的控制向量。

## Abstract
Recent advances in automatic speech recognition (ASR) have explored different sequence models, including Conformer-based models and newer state space models such as Mamba. Although prior work has evaluated these architectures in multiple languages, their effectiveness in African languages remains underexplored. In this work, we evaluate Mamba for ASR on seven South African languages. In monolingual experiments, each model is trained on 50 hours of speech per language, and we compare Mamba to a Conformer baseline of similar parameter scale. Mamba achieves similar recognition accuracy to Conformer while using fewer computational resources and training faster. We further evaluate generalization in this setting and find that both models struggle to generalize to speech that is much longer than what they were trained on. We then study multilingual ASR using Mamba models, where the baseline is pooling all languages together. On top of this, we tested three extensions: training with language-family information by adding both language and language-family embeddings as biases to the downsampled acoustic representations, and multitask learning with a CTC ASR objective and a language identification (LID) head. We find that multilingual training consistently improves performance over monolingual training. However, adding explicit language information does not improve in-domain performance but does improve cross-corpus robustness. We conducted ablation studies in low-resource multilingual settings using 5-hour and 10-hour per-language training data, where we observed gains from using language embeddings and further demonstrated that removing or altering them hurt model performance. Lastly, we analysed these embeddings and find that they do not capture linguistic similarity in a typological sense, but instead act as task-specific control vectors.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义

- **研究动机**：Mamba 作为新一代状态空间序列模型（SSM）在自动语音识别（ASR）中展现了线性时间复杂度和高效内存的优势，但在非洲语言上的评估非常匮乏。非洲语言面临“低资源双重困境”——既缺乏标注语音数据，又受限于计算资源。
- **核心问题**：系统评估 Mamba 架构在七种南非语言上的单语和多语 ASR 性能，并与主流 Conformer 基线对比；探究多语训练中显式语言信息（语言嵌入、语系嵌入、语言识别多任务学习）的作用及嵌入表征的性质。

## 2. 方法论

- **核心思想**：采用编码器-CTC 架构，将 Mamba 替换自注意力模块，实现线性时间序列建模。多语场景下通过池化数据、注入语言/语系偏置或添加辅助 LID 头来利用语言信息。
- **关键技术细节**：
  - 使用 ConMamba（集成双向 Mamba + 卷积模块 + 前馈网络），配置：18 层，hidden size=512，ffn=2048，d_state=16，expand=2，d_conv=4，参数量约 123M。
  - Conformer 基线：相同层数、hidden size 和 ffn，参数量约 114M。
  - 多语扩展策略：
    - **MI**：无语言信息的池化训练。
    - **MIF**：按语系分组训练（Nguni-Tsonga 一组，Sotho-Venda 一组）。
    - **MLE**：添加可学习语言嵌入（32维），加到 CNN 下采样后的声学表示上：\( h'_t = h_t + e_\ell \)。
    - **MLFE**：同时添加语言嵌入和语系嵌入：\( h'_t = h_t + e_\ell + e_f \)。
    - **M-CTC+LID**：多任务学习，联合 CTC 损失和 LID 损失：\( \mathcal{L} = \mathcal{L}_{\text{CTC}} + \lambda \mathcal{L}_{\text{LID}} \)，\(\lambda=0.1\)（经消融确定）。
  - 字符级建模，词汇表 189 字符，包含带变音符的字符和非标准符号。

## 3. 实验设计

- **数据集**：
  - **Swivuriso**：覆盖 7 种语言（nbl, sot, tsn, tso, ven, xho, zul），每语言 50 小时训练（短语音 0-30s），dev test 分短/长子集；此外还有拼接长语音（30s-240s）用于长度泛化测试。
  - **NCHLT Speech**：仅使用测试集（7 种语言）。
  - **FLEURS**：仅使用 xho 和 zul 的测试集。
- **基准与对比方法**：
  - 单语：Conformer（114M） vs ConMamba（123M）。
  - 多语：MI, MIF, MLE, MLFE, M-CTC+LID。
  - 额外消融：低资源（5h/10h per language）下的 MI vs MLE；语言嵌入置零和置换消融。
- **评估指标**：词错误率（WER），三点取平均，标准差作为下标。

## 4. 资源与算力

- **GPU**：NVIDIA A100（40GB 或 80GB）。Conformer 必须使用 80GB 版本，Mamba 可在 40GB 上运行。
- **训练时长**：单语模型，Mamba 约 18 小时/语言，Conformer 约 34 小时/语言。
- **训练轮数**：单语 250 epoch，多语 100 epoch。每个实验使用 3 个随机种子。
- **其他**：使用 bfloat16 混合精度，batch size=32，AdamW 优化器，学习率在 {1e-3, 8e-4, 5e-4, 2e-4, 1e-4} 网格搜索。论文未明确 GPU 数量，推测为单卡或小型集群。

## 5. 实验数量与充分性

- **实验组数**：约 6 大组实验（单语对比、长度泛化、多语策略对比、跨语料泛化、嵌入消融、低资源消融），每种子3次，总计超过 100 次训练。覆盖 7 种语言、3 个测试集、多种训练条件。
- **充分性**：
  - 单语对照公平（同配置，参数规模相近）。
  - 多语策略涵盖隐式、显式、多任务，较为全面。
  - 消融实验（嵌入置零、置换）证实了语言嵌入的作用。
  - 低资源设置模拟真实缺乏数据场景。
  - 分析嵌入余弦相似性，探索表征性质。
- **局限性**：仅使用 CTC 目标，未比较 encoder-decoder；长度泛化实验是拼接而非真实长语音；未与其他 Mamba 变体或预训练模型（如 Whisper）对比；未涉及代码切换场景。

## 6. 主要结论与发现

1. **单语性能**：Mamba 平均 WER 33.43%，Conformer 35.49%，Mamba 准确率相当但训练时间减半（18h vs 34h），且可在更便宜 GPU 上运行。Nguni 语言（nbl, xho, zul）普遍比 Sotho 语言难，可能与复杂黏着形态有关。
2. **长度泛化**：两者在长语音（>30s）上均有退化，Mamba 退化略小（+2.5% vs +2.45%），但都不鲁棒。拼接长语音实验中 Mamba 退化幅度稍低。
3. **多语训练**：所有多语策略均优于单语。MI（隐式池化）比 MIF（语系分组）好。显式语言嵌入（MLE, MLFE）在域内仅带来边际收益（0.5%），但跨语料（NCHLT, FLEURS）提升显著（平均 2.73% 和 1.08%）。多任务 LID（M-CTC+LID）反而不如 MI。
4. **低资源场景**：在 5h 和 10h 每语言下，MLE 一致优于 MI（平均降 1.88% 和 1.51%），表明语言嵌入在数据稀缺时更有用。
5. **嵌入分析**：余弦相似性矩阵不反映语言类型学关系（例如 Nguni 族内相似性不高），而是作为任务特定控制向量指导编码器优化。

## 7. 优点

- **系统性**：从单语到多语、从高资源到低资源、从域内到跨语料，实验设计层层递进，覆盖 ASR 部署的关键维度。
- **公平对比**：Mamba 和 Conformer 使用相同配置（层数、尺寸、优化器），仅架构不同，结论可信。
- **计算效率突出**：明确报告训练时间和 GPU 需求，为资源受限场景提供有价值参考。
- **消融与分析深入**：通过嵌入置零、置换实验证实有效性；余弦相似性分析揭示嵌入本质，反驳了类型学假设。
- **面向低资源语言**：聚焦非洲语言，填补了 Mamba 在此领域评估空白，并探索了低资源多语训练的有效策略。

## 8. 不足与局限

- **目标设置单一**：仅使用 CTC 损失，未评估 encoder-decoder（如 Mamba + RNN-T）或自回归解码，可能限制对长语音性能的结论。
- **长语音实验非自然**：通过拼接短语音模拟长语音，而非真实长语音数据，可能无法完全反映实际长语音中的声学变化。
- **缺乏预训练模型对比**：未与 wav2vec 2.0、HuBERT、Whisper 等预训练模型比较，而后者在低资源场景中常被使用。
- **语言覆盖不全**：南非有 11 种官方语言，本文仅涵盖 7 种，且训练数据来自单一来源 Swivuriso，可能引入领域偏置。
- **代码切换未处理**：论文提到数据集包含 code-switching 标记 [cs]，但预处理时直接移除，未针对该现象进行建模或评估。
- **硬件细节不完整**：未说明 GPU 数量和具体并行策略，复现时需推测。
- **低资源实验是采样而非真实分布**：从 50h 池中采样 5h/10h，可能与实际低资源数据（如噪声多、标注不规范）有差异。

（完）
